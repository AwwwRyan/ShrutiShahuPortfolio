# Project Memory

Running log of decisions, learnings, and draft history for the Shruti Shahu Portfolio project. Update this file whenever a decision is made, a requirement changes, or something non-obvious is learned — this is the context a fresh session (or a compacted one) needs to avoid re-deriving or re-litigating settled ground. Pair with [todo.md](todo.md) (what's next) and [README.md](README.md) (current state of requirements).

---

## Decisions log

Chronological. Each entry is a decision that's settled — don't revisit without new information.

1. **Stack:** Next.js (App Router) full-stack app, single deployable, hosted on Vercel.
2. **Database:** Postgres via Prisma ORM, provider is **Neon**, provisioned through the Vercel Storage integration (gets Neon's free tier + Vercel's env var integration).
3. **Auth:** NextAuth.js, credentials provider, single admin account (Shruti), no public sign-up. Admin provisioned via seed script.
4. **Password reset:** Email-based single-use link, 1-hour expiry, token hashed at rest, sent via Resend. Chosen over security questions / magic-link-only because it reuses infra already committed to (Resend + Prisma) and there's only one account to secure.
5. **File storage:** Vercel Blob for images and docs (cover images, gallery images, PDFs). Chosen for low traffic (<100 users/day) — no need for a managed server disk, generous free tier, zero-config with Next.js.
6. **Video hosting:** External (YouTube/Vimeo, unlisted), embedded via URL — NOT uploaded to Blob. Reasoning: sample videos run 10–33MB each; direct Blob hosting would mean real storage cost and no adaptive streaming. Blob storage stays scoped to images/docs only.
7. **Rich text editor:** Tiptap, for project descriptions and About Me.
8. **Contact form:** Working form (name/email/message) → Resend → email to Shruti. Not just static contact info.
9. **Categories are fully dynamic and multi-level** (self-referencing `parentId` on Category, unlimited depth). The four category names in the README (Writing, Editing, Marketing, Digital Journalism) are seed data only, not hardcoded. A category can hold both subcategories AND projects directly at the same time (folder-with-files-and-subfolders model) — projects are not restricted to leaf categories.
10. **Project fields beyond the original spec:** client/publication name, tags (freeform, separate from category), multiple `links[]` (each with label + url), featured flag, manual order, cover image, `gallery[]` (multi-image, for carousel-style pieces like the Digital News series), `videoUrl` (external embed for video projects).
11. **Multi-piece source documents** (e.g. the 18MB Creative Portfolio PDF, the Investimate pitch deck) go in as **one project = one doc link**, not split into per-page projects. Applies to the whole doc, not per-piece.
12. **Video script bank "KW" column → `tags` field.** `client` field stays empty for affiliate/UGC-style video projects since they're not client-attributed work.
13. **Client/brand names in real samples (MoneyHub, Investimate) are fine to display publicly as-is** — no anonymization needed, confirmed by Shruti.
14. **The MoneyHub EMI Calculator editing sample (with visible Grammarly-style feedback notes) is intentional**, not a work-in-progress artifact — it's meant to show editorial suggestion-making to recruiters. Goes in as-is.
15. **"Article - Historical Narration.pdf" is excluded** from the portfolio entirely (per Shruti — not a piece she wants shown).
16. **The 8-image Digital News series (49–56.jpg) becomes ONE project** using the `gallery[]` field (carousel), not 8 separate projects.
17. **No dedicated Writing → "Personal Essays" subcategory.** The Y2K fashion/algorithm essay and the Tradwife Trend piece fold into a "Culture" subcategory instead.
18. **No structured Work Experience/Resume data model.** The CV (skills, 8 roles, education, certifications) is NOT modeled as site content — instead it's a single admin-uploadable PDF surfaced as a "Download Resume" link/button near About Me. Keeps the data model scoped to what the original 6 requirements asked for.
19. **The CV's "Best Written Works" link** will eventually point at this new site once live — not an external site to integrate with.
20. **Workflow convention (this instruction):** Build in stages per `todo.md`, each with a test gate. Do not proceed to the next stage until the current stage's tests pass and are manually confirmed working. Every planning artifact (this file, todo.md, README.md) is treated as a living draft, updated in place rather than left stale.
21. **Admin credentials:** `shruti2004shahu@gmail.com` / a password Shruti chose herself (weak — flagged once, her call, changeable later via Stage 4's reset flow). Stored in `.env` as `ADMIN_EMAIL`/`ADMIN_PASSWORD` (seed script input only, gitignored) — the actual `AdminUser.passwordHash` in the DB is bcrypt-hashed, plaintext is never stored there.
22. **Resend delivery restriction (see Technical learnings): deferred, not fixed now.** Keep building with the current Resend setup; once the site has a real domain, verify it in Resend to unblock actual email delivery for both password reset and the Stage 8 contact form. Not a blocker for continuing Stages 5–7.
23. **Contact-form messages route to `SiteContent.contactEmail` (the public email shown on the Contact page), not `ADMIN_EMAIL`.** `ADMIN_EMAIL` is dual-purpose historically (Stage 3 seed script's admin-login email, and originally also `sendContactMessage`'s hardcoded recipient) — those two roles happened to diverge (`shruti2004shahu@gmail.com` login vs `sshahu834@gmail.com` public contact), so form submissions were silently landing in the wrong inbox. Fixed by having `sendContactMessage(name, email, message, to)` take the recipient as a parameter, sourced by the caller from `getSiteContent().contactEmail` — `ADMIN_EMAIL` now means *only* "admin login credential" again, and the delivery address stays correct automatically if Shruti ever changes her public contact email via `/admin/site-content`.
24. **Email sending switched from Resend to Gmail SMTP (via `nodemailer`), superseding Decision #22.** Confirmed live (real API call, not just docs) that Resend's free tier 403s any send to a recipient other than the account owner's own registered email (`srivastavaaryan97497@gmail.com`) without a verified sending domain — this was the actual, confirmed cause of "the emails aren't working," not a code bug. No domain was available to verify, so — per the user's explicit choice — both `sendPasswordResetEmail` and `sendContactMessage` now send via Gmail SMTP from `GMAIL_USER=srivastavaaryan97497@gmail.com` (a Google App Password in `GMAIL_APP_PASSWORD`, not the account's real login password) to `ADMIN_EMAIL`. The `resend` package was removed entirely (uninstalled with `--legacy-peer-deps` since `next-auth`'s own optional `nodemailer` peer range, for its unused Email/magic-link provider, doesn't overlap with the patched `nodemailer@^9.0.5` this project needs — irrelevant here since only the Credentials provider is used).
25. **Vercel build failure after the Resend→Gmail SMTP switch: `npm install` exited 1 with an ERESOLVE conflict.** The `npm uninstall resend --legacy-peer-deps` used locally when removing Resend (see Decision #24) only affected that one local install — it doesn't get recorded anywhere a plain `npm install` would pick up, so Vercel's deploy build (which just runs `npm install`, no flag) hit the exact same `next-auth`/`nodemailer` peer conflict and failed outright. Reproduced locally with a clean `rm -rf node_modules && npm install` (no flag) to confirm before fixing. **Fix: added `.npmrc` with `legacy-peer-deps=true`** at the repo root, so the flag applies consistently to every future install — local, CI, or Vercel — instead of relying on remembering to type it by hand. **Lesson: a one-off CLI flag used to work around a dependency conflict during a manual local operation does not persist anywhere — if the underlying `package.json`/`package-lock.json` state still needs that flag for a plain `npm install` to succeed, the flag has to live in a committed config file (`.npmrc`), or every clean install anywhere else will hit the same failure.**
26. **Stage 10 category/file assignments the original seed-content-plan bullets didn't explicitly name, resolved by judgment:** "Course Writing.pdf" → Writing/Academic (alongside the postcolonial paper). "Editing work sample.docx" (BizOps skills guide) and "Sample Editing doc.pdf" → Editing (obvious from filename/folder, just not called out by name in the plan). "Tarek El Moussa" bio and "How To Deal With Energy Trackers In Office" and "15 Digital Habits That Quietly Destroy Your Money" → Culture and Science & Psychology respectively, by topic fit. **The MoneyHub EMI Calculator draft exists as two identical file copies** (one in `Marketing And Copy Writing/`, one duplicated in `Shruti Shahu - Work Samples/Editing/`) and the plan's wording could be read as wanting it in *both* Editing and Marketing/Copywriting — but a Project belongs to exactly one Category in this schema, so it was seeded once, under **Editing**, matching the more specific and explicitly-locked Decision #14 wording ("goes in as-is... to show editorial suggestion-making"). All 26 real projects and their category placements are listed in full in the Stage 10 draft-history entry below.

---

## Video project embed URLs (confirmed)

The 6 marketing videos are now uploaded to YouTube (Shorts). These are the real `videoUrl` values to use when seeding those projects at Stage 10 — sourced from the updated `Video Script bank .xlsx` at repo root (which supersedes the older copies in `Marketing And Copy Writing/` and `Shruti Shahu - Work Samples/Best Written Stuff/`, which still only have mp4 filenames, not links):

| Project (KW)                                                | YouTube URL                              |
| ----------------------------------------------------------- | ---------------------------------------- |
| Luxury Villas in Sicily                                     | <https://youtube.com/shorts/1hKp5_FtMdc> |
| Dental Clips Guide                                          | <https://youtube.com/shorts/_9ZI2VvwAC0> |
| MRI Technician Training (TTD)                               | <https://youtube.com/shorts/7Tv3Wbli7ws> |
| All Inclusive Tanzania Safari and Zanzibar Packages (EN/ES) | <https://youtube.com/shorts/0h9xgkb7v_4> |
| Rent to own Food Trucks                                     | <https://youtube.com/shorts/JwGFaDy5JZc> |
| Rent to own ATVs                                            | <https://youtube.com/shorts/G_R_z-LJjaA> |

Remaining rows in the script bank (Best Diet for Fatty Liver, Reduce Wrinkles Around the Mouth, Early Signs of Parkinson's, How to Stop Dementia, Ireland/Scotland packages, Cancun packages, Christmas Markets in Germany) have no linked video/file — script-only, not video projects (or not yet produced).

---

## Content inventory (source files reviewed)

Reviewed in full: `Marketing And Copy Writing/` and `Shruti Shahu - Work Samples/` (34 files — PDFs, docx, xlsx, mp4, jpg). Full breakdown lives in the "Seed content plan" section of README.md — this is the short version:

- **Marketing And Copy Writing/**: Investimate pitch deck (PDF), Creative Portfolio (18MB PDF), MoneyHub EMI blog draft w/ edit feedback (docx), Video Script bank (xlsx, EN/ES rows), 6 matching mp4s in `Videos/`.
- **Shruti Shahu - Work Samples/Best Written Stuff/**: ~10 long-form writing samples (finance listicle, postcolonial lit-crit paper, archaeology, entertainment, history x2, culture/trend pieces, AI/future-of-work, workplace culture, psychology, celebrity bio) + a duplicate script bank xlsx.
- **Shruti Shahu - Work Samples/Editing/**: 5 files — BizOps skills guide, Y2K/personal-style essay, duplicate MoneyHub draft, "Article - Historical Narration" (excluded), Sample Editing doc PDF.
- **Shruti Shahu - Work Samples/Digital News/**: 8 numbered jpgs (49–56), a sequential graphic/carousel series → becomes one project.
- **Shruti Shahu - CV - Content.pdf** (repo root): Full resume — Summary, Skills, 8 roles of Work Experience, Education (BA English Lit, V.G. Vaze College 2021–2024), Certification (Udemy Editing/Proofreading/Copywriting 2023 & 2025). Not modeled as structured data — see Decision #18.

---

## Technical learnings

- **Read tool cannot open `.docx`/`.xlsx` directly** ("binary file" error). Both formats are zip archives of XML under the hood — wrote a stdlib-only Python script (no pip installs needed) at the time to extract text: `word/document.xml` for docx (`<w:t>` runs), `xl/worksheets/sheet*.xml` + `xl/sharedStrings.xml` for xlsx. Reusable approach if more Office docs show up.
- **`python3` on this machine is a broken Windows Store alias.** Use the real interpreter directly: `/c/Users/Aryan/AppData/Local/Programs/Python/Python312/python` (from Git Bash). `python-docx` / `openpyxl` are NOT installed — stick to the stdlib zip/XML approach rather than assuming they're available.
- **Read tool handles PDFs and images natively** (renders pages/images for multimodal viewing) — no extraction script needed for those, unlike docx/xlsx.
- **Markdown lint hook is active on this repo** (PostToolUse on Write) — enforces blank lines around headings and lists (MD022/MD032) and table pipe alignment (MD060). Write markdown with blank lines around every heading/list and aligned table columns from the start to avoid a follow-up fix pass; for tables, generate the padding programmatically (e.g. a quick script) rather than hand-aligning pipes.
- **Next.js 16 (Turbopack) here is newer than training data — read its bundled docs before writing app code.** `create-next-app`/`next dev` auto-generate `AGENTS.md` (imported by `CLAUDE.md` via `@AGENTS.md`) warning that APIs/conventions/file structure may differ from what's expected, and pointing to `node_modules/next/dist/docs/` as the source of truth. Consult that before writing App Router routes, data fetching, or config in Stage 2+ rather than assuming pre-16 patterns still apply.
- **`npm install` can drop mid-download (ECONNRESET) on this machine's network** — safe to just retry `npm install` in place; partial installs don't corrupt the project.
- **`create-next-app` rejects directory names starting with `_`** (npm naming restriction) — use a plain name like `scaffold-tmp` when bootstrapping into a temp folder for a merge-into-existing-repo workflow.
- **The two "work sample" folders and the CV PDF are untracked, uncommitted local files** sitting in the repo working directory (not yet in git, not mentioned in `git status` output shown at session start beyond README.md). Worth deciding, before Stage 10, whether these raw source files get committed to the repo, gitignored, or live only in Blob storage post-upload.
- **Prisma installed at v7.9.1 — a major architecture change from the 5/6 patterns in training data. Do not assume old Prisma conventions.** Concretely, on this project:
  - `prisma init` no longer just writes `schema.prisma` + a `DATABASE_URL` line in `.env`. It also generates **`prisma.config.ts`** at repo root, which is what the **Prisma CLI** (`migrate`, `generate`, `studio`) reads for the datasource URL — `schema.prisma`'s `datasource` block now only has `provider = "postgresql"`, no `url = env(...)` line.
  - The default `generator client` provider is now **`prisma-client`** (not `prisma-client-js`), and it generates a TypeScript source tree into a custom `output` path (we used `src/generated/prisma`, gitignored automatically by the CLI) — not the old `node_modules/.prisma` + `@prisma/client` re-export pattern. Import the client from `@/generated/prisma/client`, not `@prisma/client` (that package now just provides shared runtime bits).
  - **The generated client requires an explicit driver adapter at runtime — it no longer reads `DATABASE_URL` implicitly.** For Postgres: `npm install @prisma/adapter-pg pg` (+ `@types/pg`), then `new PrismaClient({ adapter: new PrismaPg({ connectionString: process.env.DATABASE_URL }) })`. This is separate from `prisma.config.ts` (CLI-only) — the app's own runtime client needs the adapter wired manually. See `src/lib/prisma.ts`.
  - `prisma.config.ts` needs `dotenv` installed (`import "dotenv/config"` at its top) since it reads `process.env.DATABASE_URL` outside of Next.js's own env-loading.
  - Vitest does **not** auto-load `.env` like Next.js does — `vitest.config.ts` needs `import 'dotenv/config'` itself, or DB-touching tests silently fail to connect.
  - `prisma migrate dev` did not auto-run `prisma generate` reliably here — ran `npx prisma generate` explicitly afterward to produce `src/generated/prisma`.
- **Testing convention for this project:** DB-touching Vitest tests run against the real (free-tier, low-traffic) Neon dev database rather than a separate test DB — each test file uses a unique per-run prefix (e.g. `` `test-${Date.now()}` `` on slugs/emails) and cleans up its own rows in `afterAll`, relying on the schema's cascade deletes to sweep up children. Keep doing this for Stage 3+ tests rather than standing up test-DB infrastructure the project doesn't need yet.
- **`middleware.ts` is deprecated in Next.js 16 — renamed to `proxy.ts`.** Confirmed via `node_modules/next/dist/docs/.../file-conventions/middleware.md`, which redirects to `proxy.md`. Same request/response signature and `config.matcher` behavior, just: file renamed `middleware.ts` → `proxy.ts`, exported function renamed `middleware` → `proxy` (default or named export either works). A NextAuth v5 `auth(...)`-wrapped handler works fine as the default export of `proxy.ts` — the rename is purely about the file/export name Next.js looks for, not the function signature. Built this project with `src/proxy.ts` from the start rather than the deprecated name. `next build` output explicitly labels it `ƒ Proxy (Middleware)` when correctly detected.
- **NextAuth v5 (`next-auth@beta`, currently 5.0.0-beta.32) App Router wiring, confirmed against the installed package's own `.d.ts` files (not assumed from training data):**
  - `src/auth.ts`: `export const { handlers, signIn, signOut, auth } = NextAuth({ providers: [...] })`.
  - Route handler at `src/app/api/auth/[...nextauth]/route.ts` must destructure: `export const { GET, POST } = handlers;` — NOT `export { GET, POST } from '@/auth'` (that fails; `auth.ts` only exports `handlers`, `GET`/`POST` live inside it).
  - `src/proxy.ts`: `export default auth((req) => { ... req.auth ... })`, with `export const config = { matcher: [...] }` same as the old middleware convention.
  - Credentials provider's `authorize()` receives `credentials` (values from the form/POST body) and must return a `User`-shaped object (`{ id, email, ... }`) or `null` — never throw for "just wrong password," return `null` instead; NextAuth turns that into a `CredentialsSignin` error at the call site.
  - In a Server Action (e.g. the `/admin/login` form's `action`), a failed `signIn()` throws — catch it, check `error instanceof AuthError` (imported from `next-auth`, re-exported from `@auth/core/errors`), then call Next.js's own `redirect()` to show an error state; rethrow anything that isn't an `AuthError` (Next.js's internal redirect-on-success also throws internally and must propagate uncaught).
  - Verified the whole flow (correct login, wrong login, protected-route redirect, authenticated access) with `curl` against NextAuth's own endpoints directly — `GET /api/auth/csrf` for a token, `POST /api/auth/callback/credentials` (`csrfToken`, `email`, `password`, `redirect=false`, `json=true`) to sign in, `GET /api/auth/session` to confirm session state — rather than needing a browser. Useful pattern for testing future auth-gated stages without browser automation.
- **Resend free tier blocks sending to anyone but the account owner's own email, until a domain is verified.** Confirmed live: `resend.emails.send({ from: 'onboarding@resend.dev', to: 'shruti2004shahu@gmail.com', ... })` returns a 403 (`validation_error`: "You can only send testing emails to your own email address (`srivastavaaryan97497@gmail.com`)..."). The Resend account was signed up with Aryan's email, not Shruti's — so any transactional email to Shruti (password reset, contact form notifications in Stage 8) will silently fail to deliver until either: (a) a real domain is verified in Resend and used as the `from` address (which also lifts the recipient restriction), or (b) Shruti's own email becomes the Resend account owner. **Not a code bug** — `createPasswordResetToken`/the whole reset flow works correctly regardless; only actual delivery is blocked. Flagged as an open decision, not silently worked around. This will also block Stage 8 (contact form) the same way — worth resolving before then, e.g. by verifying a domain once one exists for the site.
- **Server Actions can be tested over plain HTTP without a browser**, since a `<form action={serverActionFn}>` degrades to a normal multipart/form-data POST to the same URL: fetch the page, extract the hidden `$ACTION_ID_<hash>` field name from the rendered HTML, then `curl -F "$ACTION_ID_<hash>=" -F "otherField=value" -X POST <same-url>`. Confirmed working for both the forgot-password and reset-password forms this stage. Same technique will apply to Stages 5/6/8's admin forms.
- Directly testing library functions (bypassing the Next.js page/action layer) requires `import 'dotenv/config'` at the top of the script and running via `npx tsx <file>.ts` **from the project root** with a relative import (e.g. `./src/lib/prisma`) — cross-directory absolute imports from the OS temp scratchpad didn't resolve cleanly with tsx's module resolution; simplest fix is to write the one-off script inside the repo root, run it, then delete it.
- **Vitest's default 5s test timeout is too tight for tests that make multiple real round-trips to the Neon DB plus bcrypt hashing** — hit one transient timeout on a test that had passed moments earlier. Bumped `testTimeout: 15000` globally in `vitest.config.ts` rather than per-test, since this project's whole testing convention (see above) is hitting the real free-tier DB, not mocks — expect this to matter more as tests accumulate in Stages 5–9.
- **A page with no dynamic API usage of its own gets statically prerendered at build time by default, even under an authenticated `/admin/*` proxy.** `/admin/categories/page.tsx` only calls plain async DB functions (no `auth()`, no `searchParams`) — Next.js happily prerendered it (`○` in the build output) and would have served build-time-frozen category data to every visitor forever. `auth()`-calling pages (`/admin`) and pages reading `searchParams` (`/admin/login`, the reset flow) were fine, since those are themselves dynamic APIs that opt a page out of static rendering automatically. Fix: `export const dynamic = 'force-dynamic'` (confirmed current/correct via `node_modules/next/dist/docs/.../caching-without-cache-components.md` — this project isn't opted into Next 16's new `cacheComponents` flag, so the classic route-segment-config API still applies). **Checklist item for every future admin page:** if it doesn't call `auth()` or read `searchParams`/`params`/cookies itself, it needs this explicitly, or it'll silently go stale.
- **Prisma's `update()` `where` clause accepts extra non-unique filter fields alongside the unique one** (e.g. `{ id, parentId }`, not just `{ id }`) — confirmed via the generated `CategoryWhereUniqueInput` type (`Prisma.AtLeast<{...}, "id" | "slug">`). Useful as a cheap extra safety check on mutations (e.g. `reorderCategories` scoping the update to the expected parent) without a separate read-then-write.
- **Running `npm run build` immediately before `npm run dev` in the same session can leave `.next` in a state where API routes 404** (hit this with `/api/auth/*` right after a Stage 5 production build) — both commands share the `.next` directory and a stale/mixed build+dev cache broke route resolution. Fix: `rm -rf .next` before starting `dev` if you've just run `build`. Worth doing as a matter of course when switching between the two rather than debugging the 404 each time.
- **`@vercel/blob`'s server-side `put()` (used for cover/gallery image uploads) is capped at a 4.5MB request body on Vercel-hosted deployments** — fine for real photos (our reviewed samples are all under 200KB), but would break for large PDFs. Not a local-dev limit (no cap running `next dev`), and doesn't apply to a Node script calling `put()` directly outside an HTTP request (e.g. a future seed script uploading the 18MB Creative Portfolio PDF wouldn't hit this). If project doc/link uploads ever need to go through the web form for files that size, switch to Blob's **client upload** flow instead (`@vercel/blob/client` + a signed-token route handler) — deliberately not built now since it's outside Stage 6's actual scope (images only).
- **Tiptap v3 (`@tiptap/react` 3.29.2) requires `immediatelyRender: false` in `useEditor()` for Next.js SSR** (confirmed via the installed package's own overloaded type signatures — the `immediatelyRender: false` variant returns `Editor | null`, the default variant `Editor`) — omitting it causes a hydration mismatch since the editor can't render server-side. The editor's HTML is bridged into the surrounding server-action form via a controlled hidden `<input type="hidden" name="description" value={html}>` that updates `onUpdate`; the server action reads it via plain `formData.get('description')` same as any other field, no special handling needed there. `StarterKit` does NOT include the Link extension by default (separate `@tiptap/extension-link` package) — left out of the toolbar for now since links live in the separate `links[]` field already.
- **`<form action={someServerActionFunction}>` must NOT also specify `encType` (or `method`) manually** — React sets these automatically for function actions (detecting file inputs itself to pick multipart) and throws a console warning ("will get overridden") if you set them yourself. Had `encType="multipart/form-data"` hardcoded in `ProjectForm.tsx`; removed it. File uploads still worked correctly without it.
- **`ProjectForm` uses a fixed number of link label/url slots (`LINK_SLOTS = 6` in `src/lib/projects.ts`) rather than a dynamically-growable list** — deliberate choice to keep every admin form pure server-actions/no-client-JS (consistent with the whole project so far), since our real seed content never needs more than 2 links per project. Blank slots (empty label or url) are silently dropped on save (`parseProjectFields`/`createProject`/`updateProject` all filter them). Revisit if a real project ever needs more than 6 links, or when the design pass adds client-side interactivity anyway.
- **Deleting a project does not delete its Blob-stored cover/gallery images** — they become orphaned in Blob storage (harmless on the free tier at our scale, but real technical debt). `deleteCategory`'s cascade mode has the same gap for any projects it sweeps up. Not fixed now — outside Stage 6's stated scope — but worth a cleanup pass (call `del()` from `@vercel/blob` for each image) before this matters at any real scale.
- **A specific curl quoting quirk in this sandboxed Bash tool: a `-F "field=<html with angle brackets>"` value (e.g. testing the Tiptap-produced `<p>...<strong>...</strong></p>` description) intermittently breaks the request with `curl: (26) Failed to open/read local data from file/application`**, even though the value is properly double-quoted and valid shell syntax. Isolated by bisecting field-by-field — reproducible specifically when the value contains literal `<`/`>` characters. **Workaround:** write the value to a file and use curl's own `-F "field=<path/to/file"` (read-value-from-file) syntax instead of inlining HTML/angle-bracket content directly in the command string. Also learned: this environment's working directory does NOT reliably persist relative paths across every Bash tool call the way plain bash normally would (`cd /tmp` succeeded once then a later call was back at the project root) — prefer absolute paths for any file referenced across multiple tool calls, and note that native Windows Python (not MSYS-aware) can't resolve MSYS-style `/tmp/...` paths that `bash`/`grep`/`cat` handle fine — use `grep`/`cat` (MSYS-native) instead of spinning up Python for quick text extraction from HTML responses when absolute Unix-style paths are in play.
- **Internal navigation links must use `next/link`'s `<Link>`, not plain `<a href="...">`** — ESLint's `@next/next/no-html-link-for-pages` flags this (at least reliably for statically-resolvable paths like literal `href="/"`; template-literal hrefs like `` `/category/${slug}` `` weren't flagged by the linter but were fixed proactively anyway for consistency). External URLs (e.g. a Blob-hosted resume PDF) correctly stay as plain `<a>` — `Link` is only for in-app routes.
- **Category `slug` is globally unique (not scoped per-parent) in the schema, so public category URLs are single-segment (`/category/[slug]`)**, not a full hierarchical path like `/category/marketing/video-scripts/instagram`. Breadcrumbs are still computed and displayed correctly by walking the `parentId` chain server-side (`getCategoryBreadcrumbs`) — this is a URL-shape simplification, not a navigation limitation. Revisit only if SEO/URL-aesthetics ever calls for full nested paths.
- **Testing the public pages didn't require re-driving the admin HTML forms through curl** (unlike Stages 4–6) — since Stage 7 is about verifying the *read*/rendering side and the admin *write* side was already proven in Stages 5–6, test data was seeded by calling `createCategory`/`createProject`/`prisma.siteContent.upsert` directly from a one-off `tsx` script, then the public pages were checked with plain `curl` GETs. Faster and avoids the curl/shell quirks noted above (no HTML content needed in a `-F` value this time). Good pattern to reuse: use the admin-form-through-curl approach only when specifically testing the admin mutation UI itself; use direct lib calls to seed data when testing something else (like public rendering) that merely *depends on* data existing.
- **`SiteContent` has no admin UI to populate it until Stage 9** — Stage 7's public pages (Home, Contact) must and do handle a completely absent singleton row gracefully (rendered "coming soon" fallback text), since that's the real state of the live dev DB right now. Verified both the empty-state and populated-state rendering, then deleted the test `SiteContent` row afterward to leave the DB in its real (empty) current state — same one-shot-singleton cleanup care as `siteContent.test.ts`.
- **The Resend Node SDK never throws on API-level failures — `emails.send()` always resolves to `{ data, error }`, even on a 403/validation error.** Already seen indirectly in Stage 4 (the redirect still fired after a failed send), but Stage 8 made it a real correctness risk: naively wrapping `resend.emails.send(...)` in try/catch and assuming success at that point would have told real site visitors their message was sent when it silently wasn't. Fixed by explicitly checking `error` in `sendContactMessage` and throwing manually (`if (error) throw new Error(...)`) so the calling Server Action's try/catch actually has something to catch. **Rule of thumb for any future Resend call in this codebase: never trust the absence of a thrown exception — always destructure and check `error`.**
- **Verified the Resend integration is genuinely correct, separate from the known recipient restriction**, by sending a one-off test email directly to the Resend account's own address (`srivastavaaryan97497@gmail.com` — the one recipient the unverified free tier allows) via a throwaway script: real success, a message id returned, `error: null`. This isolates the two independent facts cleanly: (1) the code/API-key/SDK wiring all work correctly, and (2) delivery to any other address, including Shruti's, is blocked purely by the account-level restriction until a domain is verified — not a bug anywhere in this project's code.
- **Server Actions genuinely are "an API route" in substance** (they compile to a POST endpoint under the hood), so todo.md's Stage 8 wording ("API route sends email via Resend") is satisfied by a Server Action rather than a literal `route.ts` file — consistent with how every other mutation in this app (auth, password reset, categories, projects) is already built. Noting this explicitly since it's a interpretive call on ambiguous planning-doc wording, not a deviation from intent.
- **`todo.md`'s Stage 9 checklist under-scoped what README already committed to.** README's admin capabilities list (`- Edit contact info (email, social links)`) is a separate, explicit bullet from "Edit About Me content, upload/replace the Resume PDF" — but todo.md's Stage 9 Build list only mentioned the latter. Since both live on the same `SiteContent` singleton and would otherwise need a near-duplicate second admin page, folded contact-info editing into the same Stage 9 form rather than leaving it for an unplanned later stage. **Lesson: when todo.md and README disagree on scope, README is the source of truth** (todo.md is the derived staging plan, README is the actual requirements doc) — check both before declaring a stage's scope fixed.
- **Testing a singleton (`SiteContent`) across multiple mutation tests requires snapshot/restore, not per-test cleanup.** `siteContent.test.ts` now captures whatever the row looked like in `beforeAll` and restores those exact field values in `afterAll` (or deletes the row if it didn't exist), rather than the earlier "track whether this specific test created it" flag — the earlier pattern didn't generalize once a second test (`updateSiteContent`) also needed to mutate the same fixed-id row. General pattern for any future singleton-touching test file.
- **Stage 10's real content seeding confirmed the memory.md prediction from Stage 6/7 that a Node script calling `put()` directly bypasses Vercel's 4.5MB HTTP request-body cap** — the 18MB Creative Portfolio PDF uploaded successfully and the served copy is byte-identical (`19135424` bytes both ways). `put()` accepts a raw Node `Buffer` directly (`PutBody = string | Readable | Buffer | Blob | ArrayBuffer | ReadableStream | File`, confirmed from the package's own types) — no need to construct a Web `File` object in a script context; `readFileSync(path)` into `put(pathname, buffer, { access: 'public', addRandomSuffix: true })` is sufficient. This is a genuinely different upload path from the browser-form flow (`src/lib/blob.ts`'s `uploadFile`, which takes a `File`) — kept as a one-off inline helper in the seed script rather than merged into `blob.ts`, since it's a script-only concern (reading local disk paths), not something the running app ever needs to do.
- **Chose a consistent "short editorial blurb + doc link" pattern for every real text-based project**, rather than embedding full-length article text as the `description`. All ~13 long-form writing/editing pieces have their actual full content uploaded as the linked PDF/docx (via `links: [{ label: 'Read the full piece', url: ... }]`), with `description` holding 1–3 sentences of context. This matches how portfolio project cards are conventionally read (a pitch, then a click-through) and kept every project's rich-text field a reasonable size. Descriptions for pieces without saved verbatim extracted text were written from the title + the already-recorded content-inventory notes (see "Content inventory" section above) rather than re-reading every source file a second time — a deliberate efficiency call, not a content-accuracy risk, since titles for this batch were already unambiguous.
- **The Digital News gallery project uses `coverImage` = the first image (`49.jpg`) *and* includes that same image again as the first element of `gallery[]`** (so `gallery.length` = 9 images rendered total for 8 real source photos) — this was a deliberate choice so the carousel section shows the complete original sequence (49→56) in order, with the cover doubling as slide one, rather than skipping the image already used as the thumbnail. Confirmed via a live render count (9 `<img>` tags: 1 cover + 8 gallery).
- **First Vercel deploy failed with `Module not found: Can't resolve '@/generated/prisma/client'`.** Root cause: `src/generated/prisma` is (correctly) gitignored since it's generated code, and Prisma 7's `prisma-client` generator output isn't auto-regenerated by Vercel the way the old `prisma-client-js` generator's default location sometimes was — nothing in `package.json` ran `prisma generate` before `next build`. Fixed by adding `"postinstall": "prisma generate"` to `package.json` scripts, so it runs automatically right after Vercel's `npm install` and before the build step. **Rule for any project using a custom Prisma 7 generator output path: always add a `postinstall: prisma generate` script — don't rely on it happening implicitly in CI/deploy environments.**
- **Stage 11 hit a real "all 30 DB-touching tests suddenly fail" scare that turned out to be entirely local-machine DNS, not a code regression or a Neon-side problem.** Symptom: every Prisma call failed with `P1001 Can't reach database server at ep-winter-tooth-....neon.tech`, immediately after a `build`+`lint` pass that had been clean seconds earlier. Diagnosis path that worked: (1) confirm the connection string itself hadn't changed (asked the user to re-paste it from the Vercel/Neon dashboard — identical); (2) `nslookup <neon-host>` against the system default resolver failed ("Query refused"), but `nslookup <neon-host> 8.8.8.8` against a public resolver succeeded with valid A/AAAA records — proving the database and DNS record were fine, and the fault was the user's ISP resolver (a Reliance/Jio address, `2405:201:...:c0a8:1d01`) refusing that specific hostname while resolving everything else (including bare `neon.tech`) normally. Fix: switch the Windows network adapter's DNS server assignment from Automatic to Manual with Google DNS — **and this had to be done for both IPv4 (`8.8.8.8`/`8.8.4.4`) and IPv6 (`2001:4860:4860::8888`/`::8844`) separately**, since `Get-DnsClientServerAddress` showed Windows still using the broken ISP resolver for IPv6 even after IPv4 was fixed (IPv6 is preferred when both are present) — the first fix alone did not resolve it, only doing both did. `ipconfig /flushdns` was needed after each change for `nslookup` to reflect it. **Operational lesson for future sessions:** if a previously-passing DB-touching test suite suddenly shows universal `P1001`/"can't reach server" failures with zero code changes in between, suspect local DNS/network before suspecting the code or the database — cross-check with `nslookup <host> 8.8.8.8` before assuming anything is actually broken server-side. Also: `ipconfig` flags (`/flushdns` etc.) get mangled by Git Bash's POSIX-path translation of leading `/` — run those via the PowerShell tool instead, not Bash.
- **Design implementation pass (post-Stage-11, driven by design.md, not a numbered todo.md stage): moved the 4 public pages (`/`, `/category/[slug]`, `/project/[id]`, `/contact`) into a Next.js `(site)` route group** (`src/app/(site)/...`) so a single `(site)/layout.tsx` could inject a shared Header + Footer (with the single "Want to know more? Let's talk" CTA) without touching `/admin/*`, which stays outside the group and therefore automatically unstyled — route groups don't add a URL segment, so this was a zero-URL-impact, zero-risk restructure (confirmed via the build's route table and `proxy.ts`'s path-based `/admin/:path*` matcher being unaffected). Also removed the old `src/app/{page,category,project,contact}` files as part of the move — anything still importing them would have failed the build, and it didn't.
- **Tailwind v4's CSS-first config (`@theme inline` in `globals.css`) was extended with the locked design-token palette** (teal/navy-teal/olive-sage/chartreuse/near-black-olive/paper) as plain CSS custom properties, and `next/font/google`'s Fraunces (serif/display) + Inter (sans/body) replaced the scaffold's Geist fonts in `layout.tsx` — no `tailwind.config.js` exists or was needed (confirmed via the already-present `postcss.config.mjs` only wiring `@tailwindcss/postcss`).
- **`Category` has no `image` field and adding one was out of scope (schema changes excluded from this visual pass)** — category tiles use solid accent colors cycling through the 4 palette tones instead, which the design.md decision text already allowed for ("image or solid accent color"). Extended the shared `Card` component with an optional `imageUrl` prop (dark-gradient overlay + `object-cover`) for the cases that *do* have a real image — project cover photos on category-page project cards.
- **Chose plain text chips over real tool/software logos for the Skills section** — pulling actual Grammarly/ChatGPT/Notion/etc. brand icons would mean bundling third-party trademarked SVG assets (licensing risk, plus no bundled icon set in this project). A styled text badge in the site's own palette/type system reads as intentional rather than a placeholder, and avoids the risk entirely.
- **`generateMetadata` on the two dynamic routes (`category/[slug]`, `project/[id]`) would otherwise double the DB call** (once for metadata, once for the page component) — fixed by wrapping the existing `getCategoryBySlug`/`getProject` lib functions in React's `cache()` *locally in each page file* (not in the shared lib functions themselves, so this doesn't change behavior for any other caller). Confirmed this is the documented, current pattern via the bundled Next.js 16 docs, not assumed from training data.
- **Skeleton loading states use Next.js App Router's native `loading.tsx` file convention** (one per route under `(site)/`) rather than any custom client-side spinner state — confirmed via the bundled docs this file convention is unchanged in Next 16 (`v13.0.0` in the version-history table, no deprecation notice). Each `loading.tsx` renders a shared `<LoadingShell>` (a CSS-only animated top progress bar, `@keyframes progress-slide` in `globals.css`) plus a page-shaped skeleton built from a single `<Skeleton>` primitive (`animate-pulse`, `motion-reduce:animate-none`) — laid out to roughly match each real page's structure so the swap-in doesn't jump.
- **404 handling needed two files, not one, because of the `(site)` route group**: `src/app/(site)/not-found.tsx` catches `notFound()` calls from `category/[slug]` and `project/[id]` (both live under `(site)`, so this renders inside the Header/Footer chrome automatically) — but the bundled docs confirm only the **true root** `src/app/not-found.tsx` handles genuinely unmatched URLs app-wide, and that one renders under the root layout only, *not* the `(site)` layout, so it manually imports and wraps `<Header>`/`<Footer>` itself around a shared `<NotFoundMessage>` component to look identical either way.
- **Verified the whole visual pass live, not just via `build`/`lint`/`test`** — installed Playwright's Chromium locally (`npx playwright install chromium`; no `chromium-cli` available in this Windows/Git-Bash environment, so used `npx playwright screenshot` directly instead of the REPL) and screenshotted Home/Category/Project/Contact at both desktop (1440px) and mobile (390px) widths against the real dev server + real seeded data. Caught nothing broken, but this is the pattern to reuse for any future non-trivial styling change in this project rather than trusting `build`+`lint` alone for a *visual* task.
- **Favicon replacement: the source portrait PNG was 7944×7944 (~750KB)** — far larger than a favicon needs. Resized to 512×512 via the `sharp` package already present in `node_modules` (a transitive dependency, not one this project installed directly) using a one-off Node script, saved as `src/app/icon.png` (Next's file-convention icon, confirmed unchanged from training data via the bundled docs), and deleted the scaffold's default `favicon.ico` so there's only one icon source. **Gotcha:** Node.exe on Windows can't resolve Git-Bash-style `/c/Users/...` paths (same class of issue as the earlier MSYS-path learning) — had to use a real Windows-style path (forward slashes are fine, backslashes are not once bash string-escaping is involved) for the source file, and an absolute `require('E:/.../node_modules/sharp')` since the script itself lived outside the project tree.
- **Diagnosed a real "doc links download instead of opening" report by checking actual HTTP response headers, not guessing** — `curl -sI` on a sample of the 18 real seeded `ProjectLink` URLs showed Vercel Blob automatically sets `Content-Disposition: inline` for the 14 PDFs (browser renders them, no download) but `attachment` for the 4 `.docx` files (forces download regardless of link `target`). This is Blob's own content-type-based default, not something this codebase's upload code chose. Confirmed via a one-off `tsx` script (had to `import 'dotenv/config'` explicitly, same as `prisma/seed.ts` — plain `node -e` can't `require()` Prisma 7's generated TS-source client at all). Fix scoped narrowly: `target="_blank" rel="noopener noreferrer"` added to all external links (social, resume, doc links) site-wide; for the 4 `.docx` projects specifically, the plan is to replace the short-blurb-plus-download-link pattern with the real content in `description` (already rich text, no schema/code change needed) once Shruti supplies the text, and drop the link for just those 4 — the other 14 PDF links are unaffected and stay as-is.
- **Services section changed from a bordered-card grid to a numbered editorial list** (large serif ordinal + name + thin divider rules, no box background) after user feedback that the cards looked clickable but had no `href` — a real affordance mismatch, not just a style preference. General lesson: any static (non-interactive) content in this design language should avoid the `Card` component's visual weight/hover treatment, since `Card` implies "this is a link" by design.
- **Full dark-theme repaint scoped to the public site only, via a CSS-custom-property override on a wrapper class rather than the global `body`.** First attempt flipped `body`'s background/text tokens globally in `globals.css` — this technically also dark-mode'd `/admin/*`, since admin is unstyled and inherits `body`'s color/background directly (Tailwind v4's Preflight makes form elements inherit `color`/`background` too). Caught before shipping by remembering the explicit "admin stays out of scope" rule from earlier in this same design pass. Fix: redefine the accent CSS variables (`--color-paper`, `--color-teal`, etc.) *inside* a `.site-shell` class selector instead of `:root`, and give `SiteShell.tsx` (the shared wrapper both `(site)/layout.tsx` and root `not-found.tsx` use) that class. Because this project's `@theme inline` block maps Tailwind's color utilities to `var(--color-x)` rather than resolving them at build time, any descendant of `.site-shell` picks up the overridden values automatically — no component code needs to know which theme it's in. `:root` keeps the original light values for admin's benefit; `.site-shell` shadows them for everything inside it. **Reusable pattern for this project: any future "themed subtree" need should use this same scoped-CSS-variable-override technique, not a global token change.**
- **A second palette swap (mid-session, different reference image) turned out to require reassigning color *roles*, not just re-pointing hex values.** The first dark-theme palette had 3 bright/light-enough-to-be-text-or-fill colors (teal, olive-sage, chartreuse) plus 2 dark surface colors; the replacement reference palette had only 2 light colors and 4 dark ones. Naively keeping the same token→usage mapping (e.g. `teal` still used for `hover:text-teal` links) would have made those links nearly invisible, since `teal` was now a dark green. Fix required grep-auditing every `text-teal`/`border-teal`/`bg-olive-sage`(-as-light-fill) usage across the codebase and reassigning them to whichever tokens were actually light in the *new* palette (`chartreuse`/`paper` absorbed all the "bright accent" duty). **Lesson: when a user provides a replacement color palette mid-project, check the new palette's actual light/dark distribution before assuming a 1:1 hex swap is safe — the number of "text-safe" colors can differ between two palettes that both look reasonable as a swatch.**
- **Category tiles got real background photos (`writing.jpg`/`marketing.jpg`/`journalism.jpg`) via a hardcoded `slug → /categories/*.jpg` map in `CategoryTiles.tsx`, not a new `Category.image` DB field** — same reasoning as the earlier "no schema changes in this pass" calls: these are fixed decorative stock photos, not real content Shruti needs to swap through the CMS. Images were resized (source files up to 3.9MB) via `sharp` to a max 1600px width, preserving aspect ratio (NOT a fixed-ratio crop — `object-cover` in CSS handles the actual responsive crop per breakpoint, which a server-side square/fixed crop would have fought against). Added a `contentPosition` prop (`'start' | 'end'`) to the shared `Card` component so these tiles could anchor their label text to the top; this required flipping the image-darkening gradient direction to match (`bg-gradient-to-b` when content is at the top, `to-t` when at the bottom) — the gradient's darkest point must always land under the text, not fixed to one edge.
- **Built an in-app PDF preview modal (`DocumentLink.tsx`) after the user showed a Google-Drive-style reference.** Key realization: browsers already render PDFs natively inside an `<iframe src="...pdf">` (no viewer library needed) — the component only needed to supply the modal chrome (dark scrim, header bar with title/close/"open in new tab", Escape-to-close, body-scroll lock) around a plain iframe. Falls back to a normal `target="_blank"` link for non-`.pdf` URLs (the 4 `.docx` files still in the DB at time of writing would just show a blank/broken iframe, since browsers can't render Word docs inline — same known issue as before, unaffected by this change). **Verification gotcha:** the first Playwright screenshot of the opened modal showed a blank white pane and a `net::ERR_ABORTED` network log despite a real `200 application/pdf` response — this is `chromium-headless-shell` (Playwright's default headless browser) lacking the bundled PDFium viewer plugin, not a bug. Re-tested with `chromium.launch({ channel: 'chrome' })` against the real installed Chrome on the machine and the PDF rendered perfectly with its full native toolbar (thumbnails, zoom, page nav, download, print). **Lesson: don't trust a blank iframe/embed result from headless-shell alone when testing native-plugin-dependent content (PDFs, etc.) — re-verify against a real Chrome channel before concluding it's broken.** (Playwright was installed with `npm install --no-save` for this one test and removed from `node_modules` afterward — never touched `package.json`.)
- **Closed out the 4 `.docx`-force-download projects flagged in the earlier doc-link investigation** by moving their content into `description` (rich text) and clearing their `links[]`, via one-off `tsx` scripts following the same `updateProject`-wholesale-replace pattern used throughout. Applied differentiated treatment based on authorship: **"Nobody Has a Personal Style Anymore" and the postcolonial-analysis academic paper are Shruti's own original writing** (confirmed via her being the sole self-review commenter on the former, and an explicit byline — name/course/roll number — on the latter), so the full text went in, with her margin comments/self-critiques rendered as italicized "Editor's note" asides and flagged phrases wrapped in `<mark>`. **The MoneyHub and BizOps pieces are editing samples of third-party-authored client/blog copy** — Shruti was the editor, not the original writer — so those got a deliberately lighter treatment: a 1–2 sentence summary of the piece plus a representative list of her actual edits/suggestions and closing feedback, rather than reproducing the full underlying third-party marketing copy. Added `.rich-text mark` (chartreuse highlight) and `.rich-text blockquote`/`h2`/`h3` styling to `globals.css` to support this content structurally.
- **Replaced the four category tile images and added a real Editing photo** (previously the only one without an image) from user-supplied files in `assets/` (added to `.gitignore`, same "raw source material" treatment as the earlier work-sample folders) — resized via `sharp` into `public/categories/`.
- **Built a custom "artsy" SVG background scene for the Hero portrait** (`PortraitScene.tsx`) after the user provided a transparent-background PNG of herself and asked for something creative behind it — organic color-block shapes, a dot-grid texture, sparkles, and thin rings, all using the site's palette tokens directly (`fill="var(--color-chartreuse)"` etc.), so it automatically stays in sync with any future palette changes. The photo itself switched from the old `blob-mask` (border-radius crop, designed for opaque rectangular photos) to `object-contain` with no crop, since a transparent cutout should show its own natural silhouette rather than being force-cropped into a shape. Uploaded to Blob and set as `SiteContent.profilePhoto` (admin-editable, not a hardcoded asset — unlike the category images, this is data the CMS already models) via a one-off script using `sharp().trim()` to remove ~30% of dead transparent canvas above the subject's head before upload.
  **Real bug caught and fixed:** the background scene was completely invisible on first implementation despite correct DOM positioning and correctly-resolved CSS variable colors (confirmed via a Playwright `getComputedStyle` dump before assuming it was just "too subtle"). Root cause: the SVG had `-z-10` inside a `position: relative` parent that did **not** itself establish a new stacking context (no z-index/opacity/transform of its own) — a negative-z-index child in that situation can render behind the *page's* background rather than just behind its siblings, since it's placed in whatever stacking context happens to be nearest up the tree, which can be much further away than the immediate parent. Fix: add `isolate` to the parent (`className="relative isolate ..."`), which forces a new stacking context right there, so `-z-10` means "behind this container's own children" as intended. **Lesson: any time a decorative `-z-10` (or lower) layer seems to vanish despite correct positioning/color, suspect stacking-context escape before suspecting opacity/color — verify by bumping every value to maximum contrast first, and if still invisible, check `isolate` on the nearest positioned ancestor before anything else.**
- **Built a site-wide decorative visual system: a single scroll-drawn SVG "thread" spanning the full page height, plus a small reusable set of parallax accent motifs (`src/components/decor/`).** Key architectural choices, in order of importance: (1) **avoided negative z-index entirely** for this system, having already been burned by the stacking-context bug above — the line/motifs are simply the *first* children in `SiteShell`'s DOM order with no z-index at all, so normal document-order painting puts them behind Header/content/Footer without needing `isolate` gymnastics a second time. (2) **Parallax is CSS-variable-driven, not React-state-driven** — one `ParallaxController` (mounted once, site-wide) writes `--parallax-x`/`--parallax-y` to `document.documentElement.style` directly on `pointermove` (rAF-throttled), and every `<Motif>` just reads those vars in an inline `transform: translate(calc(var(--parallax-x, 0) * Npx), ...)` — this means zero React re-renders on mouse movement (would otherwise be a real perf problem with many motifs), and `<Motif>` itself needs no `'use client'` directive at all (purely presentational, server-renderable). Reduced-motion/touch handling is equally simple: `ParallaxController` just never attaches the listener under `matchMedia('(prefers-reduced-motion: reduce)')` or `(pointer: coarse)`, so the CSS var's `, 0` fallback keeps every motif motionless — no per-motif conditional logic needed. (3) **The weave path is generated, not hand-drawn** (`generateWeavePath.ts`, pure/deterministic function) — cubic beziers chained through a fixed oscillating x-pattern, scaled to the actual measured `document.documentElement.scrollHeight` (via `ResizeObserver` on `document.body`, so it re-measures correctly as responsive content reflows). Draw-on-scroll uses the standard `getTotalLength()` + `stroke-dasharray`/`stroke-dashoffset` technique, rAF-throttled on scroll. (4) A Playwright click-through test on a category tile first appeared to fail (URL didn't change) — turned out to be Next.js dev server compiling that route on-demand on its very first request, not a real pointer-events regression; a second run confirmed the click, navigation, and zero console errors all resolve correctly. **Lesson: a flaky-looking first-run failure right after a dev-server restart is worth a second run before treating it as a real bug**, especially for anything hitting a not-yet-visited route.
- **Service-inquiry pre-fill:** each of the 6 Services bento cards now links to `/contact?service=<slug>` instead of a bare `/contact`; the Contact page reads `searchParams.service`, looks it up in `src/lib/serviceInquiryTemplates.ts` (a `Record<string, string>` of Shruti's own verbatim per-service message templates), and passes the match as the message `<textarea>`'s `defaultValue` — an uncontrolled pre-fill, not React state, so the visitor can freely edit or delete it before sending, consistent with this project's plain-HTML-form-plus-Server-Action pattern used everywhere else.
- **`mailto:` links now carry a pre-filled subject** via a small shared `buildMailtoHref(email, subject?)` helper in `src/lib/siteContent.ts`, used by both `Footer.tsx` and the Contact page's own email link. Deliberately kept as a plain `mailto:` (not a Gmail-specific `https://mail.google.com/...compose` URL) — `mailto:` already opens whichever mail client/webapp the *visitor* has set as their own system default, which is the universally-correct behavior; hardcoding a Gmail compose link would only work for visitors who use Gmail and would be wrong to force on everyone else.
- **Email sending: confirmed live and fixed the "emails aren't working" report (see Decision #23).** A direct `curl` POST to Resend's API reproduced the exact failure: `403 validation_error — "You can only send testing emails to your own email address (srivastavaaryan97497@gmail.com)... verify a domain..."` — confirming Resend's free-tier restriction (documented since Stage 4/8) as the real, live cause, not a code regression. `GET /domains` on the account confirmed zero verified domains existed. Rewrote `src/lib/email.ts` to use `nodemailer`'s Gmail SMTP transport (`nodemailer.createTransport({ service: 'gmail', auth: { user, pass } })`) instead of the Resend SDK for both `sendPasswordResetEmail` and `sendContactMessage`; removed the `resend` package. **Peer-dependency gotcha:** `next-auth@5.0.0-beta.32` declares an *optional* peer on `nodemailer@"^7.0.7 || ^8.0.5"` (for its own unused Email/magic-link provider), which doesn't overlap with the `nodemailer@^9.0.5` version needed to clear a real `npm audit` high-severity advisory (`GHSA-p6gq-j5cr-w38f`, fixed in 9.0.5) — `npm uninstall`/`install` errored on this until run with `--legacy-peer-deps`. Confirmed safe to override: this project only uses `next-auth`'s `Credentials` provider (`src/auth.ts`), never its Email provider, so the mismatched optional peer is inert.
- **About Me content updated** (`SiteContent.aboutMe`, via a one-off `tsx` script — same wholesale-replace pattern as every other direct content edit in this project) to Shruti's new bio: 4+ years experience across ideation/execution/publication/data analysis/training, BA in English Literature from University of Mumbai (previously V. G. Vaze College — she's updated this), open to Content/Academic/Manuscript Editor roles. The Hero's hardcoded `<h1>` greeting (`Hero.tsx`, NOT part of the admin-editable `aboutMe` rich text) was also updated from "Hi, I'm Shruti" to "Hi, I'm Shruti!" to match her supplied header text exactly.
- **Writing/Marketing subcategories restructured** per Shruti's explicit new list (Editing and Digital Journalism deliberately left untouched). **Writing** went from 4 subcategories (History, Culture, Science & Psychology, Academic) to 9: History, Academia (renamed from "Academic"), Entertainment, Culture, Tech, Biographical, Psychology, Course Material, Finance. **Marketing** went from 2 (Copywriting, Video/UGC Scripts) to 3: Creative Portfolio, Pitchdecks, Affiliate Videos. Executed as a one-off `tsx` script using `createCategory`/`renameCategory`/`reorderCategories`/`deleteCategory` from `src/lib/categories.ts` plus direct `prisma.project.update` calls to reassign each project's `categoryId` — **critically, verified every project was moved out of a subcategory before deleting it**, since `Project.category` is `onDelete: Cascade` in the schema (confirmed by reading `schema.prisma` first) — deleting a category that still had a project pointing at it would have silently deleted that project's data, not just orphaned it. Cross-checked the full before/after project list against a live DB read (not memory) before running anything destructive. **Judgment calls on ambiguous splits** (none of these were explicitly specified by Shruti, since she only gave the target subcategory *names*, not a full remap): the old "Culture" pieces "Entertainment: 32 Times We Sympathized With the Bad Guys" and "Tarek El Moussa: A Biographical Deep-Dive" moved to the new **Entertainment**/**Biographical** subcategories respectively (their titles/tags directly named the new bucket); "Course Writing Sample" moved from Academic to **Course Material** (leaving the Butalia postcolonial-analysis paper alone in the renamed **Academia**); the old "Science & Psychology" bucket split four ways — "Psychology and Therapy: A Collection" → **Psychology**, "How AI Is Providing Work That Values Human Skills" and "How to Deal With Energy Trackers in the Office" → **Tech** (AI and an office gadget/tracking device both read as technology topics), "15 Digital Habits That Quietly Destroy Your Money" → **Finance** (explicitly about money), and — the one genuinely close call — "Hybrid Work Is Reshaping How America Works and Lives" → **Culture** rather than Tech, on the reasoning that it's a societal/workplace-trend piece in the same vein as the existing "Tradwife Trend" Culture piece, not really about a specific technology. **Flagging this specific placement (and the AI/Energy-Trackers → Tech calls) as the ones most worth Shruti double-checking** if the grouping doesn't read right to her — everything else in the remap was an unambiguous 1:1 rename or title/tag match.
- **Custom cursor (`CustomCursor.tsx`), scoped to `.site-shell` only.** Same architecture as `ParallaxController` — position written to CSS vars on `<html>` via a single rAF-throttled `pointermove` listener, zero React re-renders; two classes (`cursor-custom`, `cursor-hover`) toggled on `.site-shell` do all the visual work purely through CSS `transition`s (no JS animation loop). `cursor: none` is scoped under `.site-shell.cursor-custom` specifically (not applied unconditionally in raw CSS), so if JS fails to hydrate the native cursor is the automatic fallback rather than an invisible one. Skips entirely (returns early before adding any class or listener) on `(pointer: coarse)` and `prefers-reduced-motion: reduce` — verified via Playwright's `hasTouch`/`isMobile` and `emulateMedia({ reducedMotion: 'reduce' })` that `cursor-custom` never gets added in either case. Hover detection uses `pointerover`/`pointerout` + `.closest('a, button, [role="button"]')` on `document`, which needed no extra `data-*` attributes on `Card.tsx` since it already renders as a real `<a>` via `next/link`. **First implementation was a hand-drawn SVG paw print with claws that flexed out on hover** — superseded when the user supplied `assets/normal.png`/`hover.png`, two real photos of her cat (sleepy-eyes vs. alert-eyes), asking to use those instead. Both PNGs already had genuine alpha transparency (confirmed via a raw-buffer alpha scan, not just visual inspection — PNG color type 6). **Real problem found before shipping:** the source cutouts had a visible white-ish halo at their edges when composited onto the site's dark background — checked by rendering each processed candidate onto an actual `near-black-olive` swatch (matching the real site background) rather than trusting how the Read-tool's own PNG preview renders transparency (which mattes onto white, silently hiding exactly this kind of edge problem). Root cause: the semi-transparent rim pixels blend toward white rather than toward the true fur color as alpha drops (a common background-removal artifact, not a premultiplication bug fixable by simple math — decontamination via the standard `(observed - white·(1-a))/a` unmix formula was tried and rejected, since it produced colors wildly different from the actual fur tone, meaning the source alpha isn't a clean "matted-against-white" model). Fixed pragmatically for this use case (a ~56px decorative cursor, not a large hero image, so a slightly harder edge is an acceptable trade): raised the alpha cutoff to 190 (out of 255), zeroing anything below it before computing the crop bounding box, which discards the worst of the whitish rim while keeping a still-reasonably-soft edge. Manually computed the real alpha-based bounding box in a raw pixel scan rather than relying on `sharp().trim()`, which silently no-opped on these specific images (likely because trim's flood-fill algorithm doesn't handle a wide feathered edge well) — cropped, resized to 160px tall, and saved to `public/cursor/{normal,hover}.png` (the raw originals stay only in the gitignored `assets/` folder, same treatment as every other user-supplied source image in this project). **Lesson: always preview a cutout against the ACTUAL destination background color, not the tool's default white matte, before judging edge quality — a white background can fully mask a white-fringe halo that's very visible once composited onto anything darker.**
- **Real bug: `DecorativeLine` could permanently stretch every page to the height of the tallest page ever visited in that tab, after client-side navigation.** Reported by the user as a huge blank (light, base-theme-colored) gap below the footer on the Contact page after submitting the form. Root cause: `DecorativeLine` lives in `SiteShell` (the shared layout), so it never unmounts between client-side route changes — only its `useEffect`'s `ResizeObserver` (on `document.body`) is supposed to catch height changes and re-measure. But the component's own `<svg height={height}>` is *itself* absolutely positioned inside `.site-shell` with no `overflow:hidden` clipping it, so it counts toward `document.body.scrollHeight` — a self-referential measurement. Once set to a tall value (e.g. 3068px on the Home page), navigating to a much shorter page (Contact, ~1088px real content) doesn't shrink `document.body`, because the stale-height SVG is still holding it up at 3068px — so the ResizeObserver never sees a change and never re-measures, permanently pinning the SVG (and the visible gap below the real content) at the tallest height ever seen in that session, until a hard reload. Reproduced reliably via Playwright by simulating the real flow (Home → click into Contact → fill and submit the form → land on `/contact?sent=1`) and comparing `document.body.scrollHeight` to `.site-shell`'s actual rendered height — a fresh `page.goto()` per page never reproduced it (each fresh load remeasures correctly on mount), only a sequence of client-side navigations within one session did. **Fix, two parts:** (1) exclude the SVG's own footprint from each measurement — set its `height` attribute to `0` immediately before reading `document.documentElement.scrollHeight`, then restore it, breaking the self-reference; (2) add a `MutationObserver` on `document.body` (`childList`+`subtree`, rAF-throttled) alongside the existing `ResizeObserver`, since same-pathname navigations (e.g. `/contact` → `/contact?sent=1`, which only adds a success banner) don't reliably fire a body-*size* change event the `ResizeObserver` would catch on its own. **Lesson: any component that measures `document.body`/`documentElement` to size itself, and then renders something that itself occupies space in that same document, has a latent self-referential feedback-loop risk — especially in a persistent layout component that survives client-side route changes** (a fresh mount per page, which this project's `(site)/layout.tsx` does NOT do, would have masked this entirely).
- **Added `Project.externalUrl` (new schema field, real migration `20260820113556_add_project_external_url`).** When set, a project's card on its category listing page links straight to that URL in a new tab (`target="_blank" rel="noopener noreferrer"`) instead of its own `/project/[id]` page — for pieces that are really just a pointer to a client's own already-published article rather than a piece meant to have its own detail page here. `Card.tsx` gained an `external` boolean prop; `category/[slug]/page.tsx` picks `project.externalUrl` as the card's `href` when set, falling back to the internal `/project/[id]` path otherwise. **Revised shortly after shipping:** the first version rendered `external` as a plain `next/link` with `target="_blank"`, which the user correctly flagged as a regression for the PDF cases — those used to open in the site's own in-app PDF modal (`DocumentLink`, native browser PDF viewer inside a styled iframe) when reached via the old detail page, and a raw new-tab navigation lost that. Fixed by having `Card` delegate to `DocumentLink` (new `documentTitle` prop for the modal header) whenever `external` is true, instead of hand-rolling target/rel — `DocumentLink` already contains exactly the right per-URL branching (`.pdf` → in-app modal, anything else → plain new-tab anchor), so this was a strict superset of the old logic requiring zero new conditionals in `Card` itself, and it transparently keeps the psychology articles (real third-party *websites*, not files) on the correct plain-new-tab path since they don't end in `.pdf`. Wired all the way through the admin CRUD path too (`ProjectForm.tsx` input, `parseProjectFields`, `createProject`/`updateProject`), not just settable via one-off script, so this is a normal editable field going forward. **Gotcha hit immediately after migrating:** `npx prisma migrate dev` applied the DB migration fine, but the already-running `next dev` process kept its stale in-memory Prisma Client (missing the new column) even after a fresh `npx prisma generate` — `tsc --noEmit` picked up the new field correctly (fresh process), but the live dev server kept silently omitting `externalUrl` from query results until the dev server itself was restarted. **Lesson: a schema migration + `prisma generate` is not enough on its own if a dev server was already running — restart it too, or a new field can silently read as `undefined` at runtime while everything typechecks fine.**
- **PhoneMockup's `mx-auto` removal (item 4 above) was reverted shortly after — the user's original ask was actually a proper 2-column desktop layout (description one side, video the other, stacking on mobile), which the alignment tweak didn't deliver.** Re-added `mx-auto` to restore centering; the real 2-column redesign is still an open ask, not yet implemented. **Lesson: a screenshot + a short phrase like "doesn't look right" can have a much bigger intended fix behind it than the literal CSS property that looks broken — when genuinely unsure whether a fix is the full ask or just a patch, it's worth surfacing that ambiguity rather than guessing at the smallest plausible change.**
- **Gallery lightbox gained swipe navigation** (`onTouchStart`/`onTouchEnd` on the dialog, comparing `clientX` delta against a 50px threshold) after the user reported it didn't move prev/next on a phone — the click-based arrow buttons and keyboard nav from the original build had no touch-gesture equivalent. Verified via Playwright by dispatching synthetic `touchstart`/`touchend` events with explicit `identifier` fields (a bare `{ clientX, clientY }` touch object throws `Failed to construct 'Touch'` — the `identifier` property is required, not optional, despite how easy it is to omit).
- **The header/footer CTA pill ("Want to know more? Let's talk") was wrapping onto two lines on narrow phones**, per a screenshot — fixed by adding `whitespace-nowrap` plus a smaller `text-xs` (bumping to the original `text-sm` from `sm:` up) in `Pill.tsx`'s shared `BASE` class string, rather than fixed padding that couldn't flex around the fixed font size.
- **New homepage Testimonials section (`TestimonialsSection.tsx`/`TestimonialsCarousel.tsx`), UI built ahead of real content — dummy name/designation/testimony data, clearly marked as placeholder in a code comment, to be swapped once Shruti sends real testimonials.** Explicitly NOT pushed to GitHub on the user's instruction ("only after we get correct data") — committed locally as its own separate commit so the rest of the same batch of fixes (gallery swipe, pill wrapping, header logo) could still be pushed immediately; `page.tsx`'s testimonials import/usage went in this same held-back commit rather than the pushed one, since pushing it without the two new component files would have broken the build. **Real bug caught and fixed before it shipped:** the auto-scroll (`el.scrollLeft += 0.4` on a rAF loop) never actually moved anything — `scrollLeft`'s setter rounds to the nearest integer, so a sub-1px increment gets silently truncated to 0 on every single frame and never accumulates. Fixed by tracking the true fractional position in a separate ref (never read back from `el.scrollLeft`) and only writing the accumulated value into `scrollLeft` each frame. Caught by directly asserting `scrollLeft` didn't change after 2 real seconds via Playwright rather than trusting the code read as correct — the bug would have been invisible from casually watching it run in a browser tab, since "very slow and correct" and "completely stuck" look identical over a couple of seconds. Also fixed a related edge case while at it: `resume()` (on `mouseleave`/`touchend`) now resyncs the tracked position from the real `scrollLeft` first, so a manual scroll/drag while paused doesn't cause a visible snap-back to the stale pre-interaction position on the next auto-scroll frame. Chose real-`scrollLeft` manipulation over a CSS `transform`-based marquee specifically so native manual scrolling (touch drag, trackpad, scrollbar) coexists with the auto-scroll for free, matching the explicit "make it scrollable also" requirement — pausing during any manual interaction (`mouseenter`/`touchstart`) is what prevents the two mechanisms from ever fighting each other. **Refined after the user saw the live carousel:** (1) hid the native scrollbar track (new `.no-scrollbar` utility in `globals.css` — `scrollbar-width: none` + the `::-webkit-scrollbar` display-none variant, since there's no Tailwind core utility for this) — also just semantically correct given the strip loops infinitely and has no real "end" for a scrollbar to represent; (2) the seamless-loop wrap-around originally only lived inside the raf step function, so it only corrected the AUTO-scroll — a manual drag/swipe past the halfway point (the end of the first duplicated copy) would just run out of content and stop, not loop. Moved the wrap check into a native `scroll` event listener on the container instead, which fires for manual and programmatic scrolling alike; it also re-syncs the raf loop's tracked position on every scroll event, which incidentally made the old explicit resync-on-resume logic dead code, so that got simplified away too. **Real testimonials arrived and replaced the dummy data** (Sonia Sharma, Manu Sharma, Christine Wanjira Muriithi — 3 real colleagues, verbatim quotes) — verified via Playwright that all 3 names render (duplicated to 6, matching the infinite-loop content-doubling) before pushing. This was the one remaining gate on pushing the whole testimonials feature; pushed once this landed.
- **Header gained Shruti's real avatar/logo, icon+wordmark on desktop, icon-only on mobile** (`hidden ... sm:inline` on the "Shruti Shahu" text span) — the icon itself already existed as `src/app/icon.png` (the browser-tab favicon, a custom illustrated portrait, not a generic placeholder), confirmed by reading it before assuming any new design work was needed. When the user later supplied a **re-colored green version matching the site's actual palette** (`updates/icon.png`, 7944×7944 source), replaced `src/app/icon.png` (resized to the existing 512×512) and generated a new `public/logo.png` (96×96) for the Header's inline use — confirmed via a raw-pixel corner check that the source PNG's corners are already alpha-transparent (a clipped circle baked into the file, like the cat-cursor PNGs earlier), so no `rounded-full` CSS clipping was needed on top of it.
- **Four detail-page UI fixes, all flagged by the user off live screenshots.** (1) The Featured pill on category-listing cards ran into the title on the same line ("★ Featured Digital News Roundup") — root cause was `inline-block` on the pill, which doesn't force a line break the way `mb-2` alone implied; changed to `block`, which does. (2) The gallery grid was plain static `<img>` tags with no click behavior — built `GalleryLightbox.tsx` (client component) that renders the same thumbnail grid as `<button>`s opening a full-screen viewer with prev/next arrows (wrapping at the ends), an "X / N" counter, and Escape/arrow-key navigation, deliberately mirroring `DocumentLink`'s existing modal conventions (`role="dialog"`, focus-on-open, body-scroll lock) rather than inventing a new pattern. (3) The cover image on the detail page had no height cap (`w-full object-cover`, no `max-h-*`) so a tall source image could dominate the whole page — added `max-h-96`. (4) The `PhoneMockup` video embed was `mx-auto`-centered inside the `max-w-4xl` page container while all the surrounding text is left-aligned, which on wide viewports made it look like it was floating in an unrelated second column rather than the next block in a single-column stack — removed `mx-auto` so it aligns flush left like everything else; confirmed via Playwright screenshots at both desktop and a 400px mobile width that it now reads as a normal single-column stack (text → video), not a layout needing any actual reordering. **Playwright gotcha hit while verifying (2)/(3):** a naive `page.locator('img[alt]').first()` grabbed the site-wide custom cursor's `<img alt="">` (mounted early in `SiteShell`, so first in DOM order) instead of the actual cover image, making the height check read `56` (exactly the cursor's own CSS height) — scoping the locator to `main img` fixed it. Worth remembering given the custom cursor and other decor components are mounted ahead of page content in every page's DOM.
- **Real bug: the project detail page's breadcrumb only ever showed `Home / <direct category>`, skipping every ancestor in between** (e.g. "Home / Affiliate Videos" instead of "Home / Marketing / Affiliate Videos") — spotted by the user on a screenshot. Root cause: `project/[id]/page.tsx` built its breadcrumb from `getProject()`'s own `category: true` include (just the one direct `Category` row), while the category listing page (`category/[slug]/page.tsx`) has always correctly used `getCategoryBreadcrumbs(categoryId)` to walk the full ancestor chain to the root. The project page simply never adopted that same helper. Fixed by calling `getCategoryBreadcrumbs(project.categoryId)` on the project page too and rendering the same `breadcrumbs.map(...)` pattern already used on the category page, instead of the one-off `project.category.name`/`.slug` render. **Lesson: this project has two different pages that both need "full ancestor breadcrumb trail" and only one of them was ever wired to the shared helper that computes it correctly — worth checking, any time a new page shows a category/hierarchy path, whether it should be calling `getCategoryBreadcrumbs` rather than reading one relation level and assuming that's enough.**
- **The 4 previously-flattened-to-text docx projects (Nobody Has a Personal Style Anymore, the Butalia postcolonial paper, MoneyHub, BizOps) now match the "Read the full piece" pattern exactly: card opens the PDF directly via `externalUrl`, no detail page in the flow at all.** First implementation (kept the curated `description` as the primary page, with the comment-PDF as a supplementary `ProjectLink`) was explicitly corrected by the user — they wanted the same "click card → PDF opens, no intermediate page" behavior as the 11 "Read the full piece" projects, full stop, not a richer detail page with a link out. Converted via the identical migration shape already used for those 11: copy the `ProjectLink.url` into `Project.externalUrl`, delete the now-redundant `ProjectLink` row. No new code needed — the `external` → `DocumentLink`-in-`Card` mechanism and `getAllDocUrls()`'s `externalUrl` scanning already handle this generically; only the data changed. Getting the actual comment-bearing PDFs onto Blob first (see below) was still worthwhile even though the "keep the description page" framing didn't survive — the PDFs themselves are the real deliverable, the surrounding page structure was the part that got revised. Background on how those PDFs were produced: Word comments can't be shown in a browser at all (no native `.docx` rendering), and the free Microsoft Office Online embed trick was tested against a real doc with actual comments (`Editing work sample.docx`, confirmed via its `word/comments.xml`) and found to render the page fine but expose **no comments pane or toolbar whatsoever** — a real, confirmed limitation of that free route, not a config issue. **Google Docs was also ruled out** before any implementation: it has no export path (UI or API) that bakes sidebar comments into a PDF the way Word's own "Document showing markup" PDF option does — a well-documented limitation, not something worth guessing about. The user exported the 4 docs from real Word instead (confirmed via the resulting PDFs — genuine track-changes strikethroughs/insertions plus margin comment balloons, visually verified via Playwright screenshot before wiring anything up). One doc (the Butalia paper) has no actual Word comments in its source file (confirmed via a missing `comments.xml`), so its Google-Docs-exported PDF is clean text with no markup — expected and correct, not a defect, just a card that opens a plain (if complete) paper rather than an annotated one.
- **11 "Read the full piece" projects converted from an internal detail page + PDF link to a direct-open card, reusing `externalUrl`** (not a new mechanism — the same field/behavior built for the psychology articles). Identified by the user via the exact link label text; queried the DB for every project whose `links[]` contained a link labeled precisely `"Read the full piece"` (11 matches, all in Writing subcategories) rather than guessing by project shape — three other single-PDF projects with differently-worded labels ("Read the before & after" on the Institute for Sexual Research editing sample, "View the full portfolio" on Creative Portfolio, "View the pitch deck" on Investimate) were initially left untouched since the user's ask was scoped to that specific label text, not "every project with just one doc link" — **but the user later spotted the Institute for Sexual Research detail page still existing and asked why, confirming the real intent was the latter, broader rule.** All 3 were converted the same way in a follow-up pass — same migration shape, same verification approach (precise `a[href="/project/<id>"]` check per category, confirmed each card now opens its PDF in the modal directly). **Lesson: when a user scopes a request to a literal exact match (a specific label string) but the underlying intent is really "every project of this shape," the literal-match version can look complete while quietly leaving a few inconsistent stragglers — worth flagging the near-miss cases explicitly rather than silently applying the narrowest possible reading and letting the user find the gaps one at a time.** For each match: copied the link's URL into `externalUrl`, then deleted the now-redundant `ProjectLink` row. Doesn't delete the `Project` row or the `/project/[id]` route itself (still returns 200 if visited directly) — "drop the page" was interpreted as *nothing links to it anymore*, not *delete the record*, matching how `externalUrl` already made a project's own detail page unreachable via normal navigation for the psychology pieces. **Follow-on fix required:** `getAllDocUrls()` (the doc-preload queue's source list) originally excluded `externalUrl` entirely, on the assumption it only ever points at a third-party page we don't host — true for the psychology articles, but now false for these 11, whose `externalUrl` points at a PDF we host on our own Blob storage. Broadened it to also scan `Project.externalUrl` for the `.pdf`/`.doc`/`.docx` extensions, so these 11 documents keep getting prefetched by `DocPreloadQueue` exactly as before the migration — verified the total unique doc count stayed at 15 (same files, just relocated from `ProjectLink.url` to `externalUrl` for 11 of them). Verified via Playwright with a precise `a[href="/project/<id>"]` check (not a raw substring search — an early version of this check false-positived because the migrated projects' cover-image filenames happen to embed their own project ID) across every affected category page that no internal link to any of the 11 detail pages remains anywhere on the site.
- **Real content migration: cover images added to 15 existing projects, "Nobody Has a Personal Style Anymore" moved from Writing/Culture to Editing, and the old single "Psychology and Therapy: A Collection" project replaced with 4 separate external-link articles** (Codependency/Teen Anxiety/Trauma Recovery/Trauma Bonding, each an `externalUrl` pointing at the real published piece on bhbehavioralassociates.com — this confirms and closes out the original "this doc has links I want to show outside the pdf" request from earlier in the project). Source screenshots came from a user-populated `updates/{editing,writing,writing/psych}/` folder (filenames matching project titles closely enough to map by hand, cross-checked against a live DB dump — not memory — before running anything). Uploaded via a one-off script using `put()` from `@vercel/blob` directly on a `Buffer` read via `node:fs/promises` `readFile` (same pattern as the original Stage 10 seeding), not the browser-FormData-oriented `uploadFile()` helper. **Also swapped `SiteContent.resumeUrl`** to a new CV file the user had dropped in the same `updates/` folder (`Shruti Shahu - CV - Content.pdf`) — this one required an explicit follow-up confirmation from the user before touching it, since flagging its presence twice earlier hadn't been enough signal on its own to act on.
- **Homepage now prefetches every project's cover/gallery image** (`src/lib/media.ts`'s `getAllImageUrls()` + `src/components/MediaPreloader.tsx`, mounted in `(site)/page.tsx`) via `<link rel="prefetch" as="image">` tags — Next.js hoists any `<link>` rendered anywhere in the tree up into `<head>` automatically. Addresses a real reported delay: category/project images previously only started downloading the moment a visitor actually navigated to a page that used them; now the browser's HTTP cache is already warm by the time they get there, since `prefetch` is the lowest-priority fetch type and never competes with the current page's own requests. Deliberately excludes the profile photo (already loading at full priority via Hero on the very page this mounts on — prefetching it too would just be a redundant duplicate).
- **Doc/PDF preloading (`src/components/DocPreloadQueue.tsx`), added after the bandwidth tradeoff above was surfaced back to the user — their call was "preload small ones first, then larger ones, as a queue."** Real difference from `MediaPreloader`: this can't just be a burst of declarative `<link rel="prefetch">` tags, since some of these documents are tens of MB and the whole point is NOT hammering the connection with several large downloads at once. Implemented as an actual client-side queue: on mount, `HEAD`-request every doc URL in parallel to read `Content-Length` (falling back to `Infinity`, i.e. sorted last, if a HEAD fails), sort ascending, then `fetch()` each one **sequentially** (`cache: 'force-cache'`, awaiting each before starting the next) so only ever one large download is in flight at a time and the fastest/smallest documents land in cache first. Skips entirely when `navigator.connection.saveData` is set or `effectiveType` is `2g`/`slow-2g` — this is a pure nice-to-have, never worth spending someone's metered mobile data on. `getAllDocUrls()` (`src/lib/media.ts`) sources every `ProjectLink.url` and `SiteContent.resumeUrl` ending in `.pdf`/`.doc`/`.docx` — deliberately excludes a project's `externalUrl` and any non-file reference link (e.g. "read the original on their site"), since those point at pages this site doesn't host and prefetching someone else's full webpage isn't the same thing (and could fail on CORS grounds having nothing to do with our own Blob storage's permissive public-object CORS). Verified via Playwright request-log capture (not just "no console errors") that `HEAD` fires for all 15 real docs found in the live DB, then a `GET` follows for each once sizing completes — confirmed the queue actually reaches the fetch phase, not just the sizing phase.
- **Contact form submit button gained a pending/loading state (`src/components/ContactSubmitButton.tsx`)** via `useFormStatus()` from `react-dom` — the one piece of this form that *needs* to be a Client Component, since `useFormStatus` only works inside a descendant of the `<form action={...}>` it's tracking. Kept as a small standalone component rather than converting the whole Contact page to a client component, preserving the established plain-HTML-form-plus-Server-Action pattern everywhere else on the page.
- **`node_modules` corruption recurred a third time** (all 8 vitest files failing with the same `Cannot read properties of undefined (reading 'config')`, "Test Files 8 failed (8)", full-suite-only) even with no leftover `node_modules/playwright` directory this time — same fix as before (`rm -rf node_modules && npm install`) resolved it in one pass (49/49 passing after). **Lesson reinforced: treat any full-suite-only, single-error-message-across-every-file test failure as a `node_modules` state problem first**, and re-verify with a clean reinstall before investigating the code — this has now happened 3 times in this session with no code-level cause found any of the times.
- **Reworked the Services section into a responsive bento grid** (asymmetric cell spans — one large featured cell, one tall cell, several regular ones) after the user shared a reference mockup. The mockup's cards also had per-service description copy and a "MOST REQUESTED" badge — deliberately **left both out** since neither was previously-approved real content (the original Locked decision only specified the 6 service names, no descriptions, and "most requested" would be an unverifiable factual claim about Shruti's business) — implemented the mockup's *layout pattern* only, keeping the existing approved names/icons. Responsive strategy: complexity scales up with viewport width rather than trying to force the intricate desktop bento shape into a narrow screen — mobile is a plain single column with no spans at all, tablet is a simple 2-column pairing (with the wide "hero" cards still full-width), and the full asymmetric bento (`grid-cols-4` + `row-span`/`col-span` + `grid-flow-dense`) only applies at `lg:`.
- **`/admin` visual redesign: applied the public site's design system (dark palette, Fraunces/Inter, Pill/Card conventions) to all 9 admin routes, which had been deliberately plain unstyled HTML since the original design pass locked `/admin` out of scope.** New `.admin-shell` class in `globals.css`, a **sibling** to `.site-shell` (not a reuse — several existing comments in that file and `SiteShell.tsx` explicitly asserted admin stays excluded, and reusing the class would have made those comments false), duplicating the same dark palette CSS vars via the same scoped-custom-property-override technique from the earlier dark-theme pass (see the `.site-shell` entry above). Added one new accent, `--color-rust` (`#c46d64`), for destructive actions — the first departure from the locked 6-color palette, deliberately narrow (danger UI only), decided via `AskUserQuestion` before implementing. **Contrast rule that mattered:** solid rust fills (danger buttons) must use `text-near-black-olive` (4.68:1, passes AA), not `text-paper` (3.08:1, fails) — rust as foreground text/icon color on the dark background is fine either way. New `src/lib/adminStyles.ts` (plain exported class-string constants, matching the project's existing lightweight `const inputClasses = '...'` pattern rather than a component library) and `src/components/admin/{AdminHeader,AdminNav}.tsx` (server component calling `auth()` itself, same independent-fetch pattern as `Footer.tsx` calling `getSiteContent()`; nav is a small `'use client'` component using `usePathname()` purely for active-link styling). Styling-only changes on top of the existing Server Action wiring **except** two deliberate behavior additions the plan called out explicitly: surfaced two previously-silently-swallowed category errors (`CircularMoveError` on move, empty-name no-op on create/rename) via `redirect(...?error=Code)` + a styled alert banner — the original code comment on `moveCategoryAction` literally called this "a design-pass concern," so this was in scope, not creep.
- **Gotcha: a `<fieldset>` with a `<legend>` gets a browser-drawn notch cut into any border painted directly on the fieldset itself** — avoided by design (card border goes on a wrapping `<div>`, `<fieldset className="border-0 p-0">` inside), per the approved plan. **Separate, unplanned bug this surfaced during verification:** `<fieldset>`'s *other* UA default, `min-width: min-content` (not reset by Tailwind's Preflight), was forcing the whole page wider than the viewport on `site-content`/`ProjectForm` whenever a fieldset contained an unbreakable long string (a full Blob URL) — defeating a nested `truncate` utility and producing a real horizontal-scroll regression, caught via a screenshot showing a thin light strip at the viewport edge, not by reading the code. Fixed with `min-w-0` added to every such fieldset (8 occurrences across both files). Verified fixed by asserting `document.body.scrollWidth === window.innerWidth` via Playwright, not just "the strip looks gone."
- **Real pre-existing bug found and fixed during admin redesign verification: NextAuth `signIn()`'s default `redirect: true` swallows a bad-credentials failure silently — it performs its own internal `redirect()` on both success *and* failure and never throws anything for a `try/catch` to catch.** Confirmed by reading `node_modules/next-auth/lib/actions.js` directly rather than assuming from training data (this project's `next-auth@5.0.0-beta.32`). The original `/admin/login` action's `try { await signIn(...) } catch (error) { if (error instanceof AuthError) redirect(...) }` was therefore dead code — confirmed via `git diff` that this exact logic was byte-identical before the redesign touched only surrounding JSX/imports, i.e. a **pre-existing bug that had presumably never actually shown a wrong-password error to a real user**, not a regression introduced by the styling pass. **First fix attempt was also wrong:** removing the try/catch and just adding `redirect: false` isn't sufficient — with `redirect: false`, `signIn()` *does* throw a real, catchable `CredentialsSignin extends AuthError`, and removing the catch turned that into an uncaught 500 (seen in dev server logs). **Correct fix:** keep `redirect: false` **and** wrap in `try/catch` catching `AuthError`, redirecting manually on failure (`?error=1`) or success (`/admin`). **Lesson: for NextAuth v5's Credentials provider in a Server Action, `redirect: false` is not optional if you need to distinguish success from failure yourself — the default `redirect: true` mode is fundamentally uncatchable by design, not just "usually fine."**
- **Real, pre-existing production data-integrity bug found and fixed during admin redesign verification: the "Move up"/"Move down" reorder buttons for both categories and projects were silently no-ops for any sibling group that had ever had a member deleted.** `createCategory`/`createProject`/`moveCategory` all assigned a new/moved row's `order` as `COUNT(existing siblings)` — correct only if no sibling in that group has ever been deleted; once one is, the surviving siblings' `order` values don't shift down, so a later create/move can land on an `order` value a surviving sibling already holds. `moveSibling`/`moveProjectSibling` then swap the mover with whichever row sits at its immediate array index in a `order asc`-sorted list — when two rows share a duplicate order value, the tiebreak (arbitrary/insertion-order-ish) can put the "neighbor" at the wrong array slot, and swapping two rows that already hold the *same* order value is a real no-op (value written back is identical to what was already there), even though the button, form POST, Server Action, and `revalidatePath` all fire and return 200 with no visible error. **Confirmed live in the real (Neon) production database**, not just synthetically: one top-level category ("Digital Journalism") and 13 real projects across several categories already had duplicate/non-contiguous `order` values, evidently accumulated over the project's history of admin edits/deletions well before this session. **How this was diagnosed** (worth the process note, since two wrong theories were tested and ruled out first): (1) suspected pure Playwright test-timing flakiness at first, matching several *other* genuine false negatives hit in the same verification pass (see below) — ruled out by polling/retrying the check and by cross-checking against a direct DB query, which still showed zero change. (2) suspected a stale Next.js Fast Refresh–compiled Server Action module, given the dev server had been running through ~2 hours and 17 file edits — ruled out by fully killing and restarting the dev server (found via `netstat`+`Get-CimInstance Win32_Process` to confirm it really was `next dev`, not some other listener) and reproducing the identical no-op immediately after. (3) Only adding a temporary `console.log` inside `moveSibling` itself and reading the real dev-server terminal output showed the actual sibling list with the duplicate/gap `order` values, making the root cause direct and undeniable rather than inferred. **Fix, two parts:** (a) root cause — `createCategory`/`moveCategory`/`createProject` switched from `count()` to `aggregate({_max: {order: true}})` + `+1`, which stays unique regardless of deletion history; (b) self-healing — `moveSibling`/`moveProjectSibling` now renumber the *entire* sibling group to a clean `0..N-1` sequence (one transaction, N updates) instead of swapping two `order` values directly, so any group's ordering is always corrected the next time anyone reorders within it, even if new corruption somehow occurs. (c) also ran a one-time repair script directly against the live DB, grouping every category/project by parent/category and renumbering any group with `order` drift — this is the same 1 category + 13 projects mentioned above; already-clean groups were left untouched (the script only writes rows where `from !== to`). **Lesson: "the button does nothing and returns 200" is not proof of a client-side/test problem — when a Server Action's own business logic can produce a false "no-op success," add a debug log and read the real server output before trusting either a passing OR failing test result.**
- **Several Playwright verification false negatives hit during this same admin-redesign testing pass, none of them real bugs — worth remembering as a set, since multiple showed up independently and each briefly looked like a regression:** (1) **Repeated `page.goto()` to the exact same URL within one Chromium session can serve a stale disk-cached HTML document**, even though this project's dynamic admin pages send `Cache-Control: no-cache, must-revalidate` — because there's no `ETag`/`Last-Modified` validator in the response, "no-cache" doesn't behave like `no-store` the way the header name suggests, and a second `page.goto()` to the identical URL can render pre-mutation state (e.g. a "Move here" `<select>` missing a category created moments earlier in the same script). Fix: append a cache-busting query string (`` `?_=${Date.now()}` ``) to any repeated navigation used purely for verification. (2) **A plain-HTML-form POST causes a real page reload where the DOM commit can lag a few ms after Playwright's `networkidle` fires** — a single-shot `.isVisible()`/`.count()` check immediately after clicking a submit button can read the pre-update DOM. Fix: a short polling retry helper (`waitTrue(fn, timeout, interval)`) around any post-mutation assertion, rather than one-shot checks — this alone turned several false "FAIL"s into "PASS" with zero code changes. (3) **`locator.filter({ has: locator }).getAttribute(...)` (or `.textContent()`) on a selector that matches more than once throws a Playwright strict-mode violation — and a blanket `.catch(() => null)` silently converts that thrown error into a false "attribute not found," masking a real feature as broken.** Hit twice this session in different testing contexts (an alert-role check that also matched Next.js's own route-announcer element; a social-link check that matched the same link rendered once in main content and once in the footer) — both times the underlying feature was working correctly. Always add `.first()` or otherwise disambiguate a multi-match locator before wrapping the call in a `.catch`, or verify the "not found" result some other way before trusting it. (4) **Clicking the wrapper `<div>` around Tiptap's `EditorContent` doesn't reliably focus the actual `[contenteditable="true"]` ProseMirror node** for subsequent `page.keyboard.type()` calls — typed keystrokes can silently go nowhere, leaving an empty `<p></p>`. Fix: target `.rich-text [contenteditable="true"]` specifically, not the outer `.rich-text` wrapper, before typing.
- **Added 3 more testimonials** (Shruti T / PR & Culture Writer, Satyajit Mallick / Head of Operations, Kshitija Shetty / Writer & Trainee Psychologist) to `TESTIMONIALS` in `TestimonialsSection.tsx`, bringing the total to 6. Satyajit was explicitly placed 2nd (not appended last) per the user's specific ask, since he was Shruti's direct manager and she wanted his testimonial to have that prominence — order otherwise preserved (existing 3 kept their relative order, the other 2 new ones appended at the end). The carousel's duplicate-for-infinite-loop logic (`[...testimonials, ...testimonials]`) is length-agnostic, so no component changes were needed, just the data array.
- **Writing category's subcategory grid was capped at `sm:grid-cols-2` (2-wide even on large screens)** — with 9 real subcategories this made for an oddly narrow, tall list. Changed to `sm:grid-cols-2 lg:grid-cols-3` on `category/[slug]/page.tsx`'s Subcategories grid (the same cap the Projects grid on that same page already used), giving a clean 3×3 at desktop widths. This is the shared template for every category's subcategory grid, not Writing-specific — applies uniformly, which is fine since no other category has few enough subcategories for 3 columns to look sparse.
- **Added real cover images to 9 more projects that previously had `coverImage: null`**, from a second batch of user-supplied files dropped into `updates/` (`updates/editing/Nobody has a personal style anymore.jpeg` — a Y2K-bedroom photo for the personal-style essay — and 8 files under `updates/markettnig/{creative portfolio,pitch deck,videos}/`, real marketing banners/thumbnails for the Creative Portfolio and Investimate pitch decks plus the 6 affiliate video projects). Matched filename → project by exact title/keyword against a live DB query first (not assumed), confirming all 9 targets genuinely had a null cover before touching anything. Uploaded via the same established one-off-script pattern as the original Stage 10 seed (`put()` from `@vercel/blob` on a `Buffer` from `readFile`, pathname `projects/covers/<filename>`, `addRandomSuffix: true`) rather than the browser-form `uploadFile()` helper (script context, no `File`/FormData available) — then `prisma.project.update` set each `coverImage` directly. Verified all 9 Blob URLs resolve (200) and separately confirmed via real `curl` fetches of the live dev server that each image actually renders on its real category page (Affiliate Videos, Editing, Creative Portfolio, Pitchdecks) — not just that the DB write succeeded. Pure content/data change, no code touched, so nothing to commit for this one.
- **Set up a Neon dev branch so local `next dev` no longer mutates the real production database** — this had never been addressed before now; every local admin-panel test all session had been hitting the live Neon DB directly, requiring careful create/cleanup discipline (`ZZ_TEST_*` naming, singleton snapshot/restore) rather than a real sandbox. User created the branch via the Neon Console (parent = production, data included) and swapped `.env`'s active `DATABASE_URL` to it, commenting out the production one — a deliberate one-file, manual-comment-toggle choice over a two-file (`.env` + `.env.production.local`) split, since they didn't want to manage a second file. **Key mental model that resolved their actual confusion ("how does my system know which branch to use"):** local `next dev` and the Vercel-deployed production site read env vars from two storage locations that never talk to each other at all — local always means "whatever's uncommented in the `.env` file on this machine," Vercel production always means "whatever's configured in Vercel's own dashboard," and neither can see the other since `.env` is gitignored and never reaches Vercel. There's no "switching" logic anywhere; it's just two independent values that happen to share a variable name. Verified genuine isolation with a real script (not assumed from the URLs looking different): confirmed the dev and prod hostnames actually differ, confirmed the dev branch has the real data (16 categories/29 projects, matching prod exactly), then created a throwaway category against the dev connection and confirmed via a *separate* direct connection to the prod URL that it does not exist there — real proof, not just "the branch exists so it must be isolated." **Gotcha hit immediately after the `.env` edit:** the already-running `next dev` process kept using the old (production) `DATABASE_URL` silently, since env vars are only read once at process startup, not hot-reloaded on file change — had to fully kill and restart the dev server before the swap actually took effect. Same class of gotcha as the earlier Prisma-schema-migration one (see the `externalUrl` entry above): editing a file that only gets read at startup does nothing to an already-running process, and there's no warning that this happened — it just silently keeps working against the old value.
- **Real bug found via the user's own live testing (not this session's own verification pass): submitting the site-content form (profile photo + resume PDF together) threw `Body exceeded 1 MB limit`, a Next.js Server Action hard default.** `next.config.ts` had never set `experimental.serverActions.bodySizeLimit` (config was still the literal `create-next-app` scaffold placeholder, `{/* config options here */}`), so it was silently capped at Next's 1MB default the whole time — the site-content form (photo + PDF) and `ProjectForm` (cover + multiple gallery images) can both realistically exceed that in one submission. **Chose `4mb` deliberately, not an arbitrarily larger number:** Vercel's serverless functions have their own hard 4.5MB request-body ceiling regardless of what Next.js is configured for (see the earlier `@vercel/blob` 4.5MB entry above) — setting this higher than ~4mb would just mean Vercel itself rejects the request before this config value is ever consulted, so 4mb is effectively the real usable ceiling for *any* browser-form upload on this stack, not a conservative guess. Verified live (not just "should work now"): generated real ~900KB/~500KB dummy photo/resume files (deliberately sized so their combined multipart body clears the old 1MB cap but stays under the new one), submitted through the real running form via Playwright, confirmed a clean `200`/`?saved=1` with no error banner — then restored the real profile photo/resume URLs immediately after, since this ran against the singleton `SiteContent` row (on the new dev branch, not production, per the item above — first real payoff of having that isolation in place).
- **Real, user-triggered bug caught live: editing a project and touching the flat `categoryId` `<select>` (all 18 categories/subcategories in one same-looking alphabetical list, no hierarchy shown) silently reassigned the MoneyHub EMI Calculator project from Editing to Finance (a Writing subcategory), plus its header picked up a stray "Edit - " prefix from the same testing pass — Editing's admin page correctly stopped showing the project once its `categoryId` changed, which read as "it vanished" even though it was just sitting somewhere else in the tree.** Diagnosed by directly querying the DB for the project by title (not guessing) and reading its actual current `categoryId`/`header`/`updatedAt` — confirmed it wasn't lost, restored the two touched fields (`header`, `categoryId`) via a targeted `prisma.project.update`, leaving every other field (description, cover, links, tags, order) untouched by first reading the full row. Happened on the new dev branch (see above), so zero production impact — a real, immediate payoff of having that isolation in place before this kind of mistake happened. **Root-caused and fixed properly, not just restored:** the actual problem was the picker itself — a flat list mixing top-level categories and deeply-nested subcategories with identical styling and no path context makes a wrong click easy and its result invisible until you go looking for the "missing" project elsewhere. Presented the user 3 concrete fix options via `AskUserQuestion` (show full path in each option; group by top-level with visual sections; restrict the list to the project's current tree, with an explicit control to cross trees) — user picked the last. Built `src/components/admin/CategoryTreeSelect.tsx` (small `'use client'` component, the first client-side filtering UI in `/admin` beyond `AdminNav`'s pathname check): a "Section" `<select>` (top-level categories only) scopes a second "Category" `<select>` (`name="categoryId"`, the only one the server actually reads) to that section's full subtree, walking `parentId` chains generically rather than assuming a fixed 2-level depth (current real data happens to be 2 levels, but the schema/admin tree UI both support arbitrary nesting). Defaults: Section = the project's current top-level ancestor, Category = its exact current category — and critically, **changing Section resets the Category default to that section's top level rather than trying to preserve an now-irrelevant old value from a different tree** (`key={selectedRoot}` forces the second `<select>` to remount with a fresh `defaultValue` on every Section change), while switching back to the *original* section still shows the project's exact original subcategory, not just the section's top. `useCallback`-wrapped the `rootOf`/`depthOf` tree-walk helpers to clear a real `react-hooks/exhaustive-deps` warning rather than leaving it (matching this project's established zero-warnings standard from the earlier `GalleryLightbox` fix). Verified live end-to-end via Playwright: Section/Category both correctly default to a project's real current category on the edit page; the Editing section correctly shows just itself (flat, no subcategories) rather than the full 18-option list; switching Section to Writing correctly repopulates with all 9 Writing subcategories (indented per depth) and Editing disappears from the list; switching back to Editing correctly re-scopes without any Writing-tree leftovers; and — the actual regression test for the original incident — **opening the edit page and clicking Save with zero changes correctly leaves the project exactly where it was**, which is the specific failure mode that caused the original "vanishing" report. Also confirmed the `/admin/projects/new?categoryId=...` pre-fill path (the form's other caller, from clicking "Add project" on a specific category card) still resolves both Section and Category correctly from the query param.
- **Added direct file upload for `externalUrl`, closing a real gap: the field had only ever been settable by hand-typing a raw Blob URL (or via one-off seed scripts) — there was no way for the admin to actually swap the linked PDF for a project like MoneyHub through the UI itself.** New `uploadExternalDoc(formData)` in `projectFormData.ts` (same `uploadFile(file, folder)` pattern as `uploadCoverImage`/`uploadGalleryImages`, folder `projects/docs` matching the path convention already visible in existing `externalUrl` values from the original seed). `ProjectForm`'s External URL section now shows the raw URL text input *plus* a file input below it ("Or replace it by uploading a new file") — a new upload, if provided, overrides whatever's in the text field (`externalUrl: newExternalDoc ?? fields.externalUrl` in both `createProjectAction` and `updateProjectAction`), wired alongside the existing cover/gallery upload `Promise.all`. Verified live end-to-end: uploaded a real minimal PDF through the actual running edit form for the (recently-restored) MoneyHub project, confirmed the External URL field showed a genuinely new Blob URL afterward (not the old one), confirmed that URL resolves with a 200 and its bytes exactly match the uploaded file (not empty/corrupted) — then restored MoneyHub's original document URL immediately after (same singleton-style care as the other tests today), and separately confirmed the *original* PDF Blob object is still live too (uploads don't delete anything, so nothing was lost by the test).
- **Added `Project.showDescriptionPage` (new schema field, real migration `20260821112147_add_project_show_description_page`, applied to the dev branch), decoupling "does this project have an externalUrl" from "does its card open that URL directly."** Previously the two were the same thing — setting `externalUrl` *always* made the card bypass its own `/project/[id]` page, with no way to keep a document attached/uploaded while still showing the description page (or vice versa) without deleting and re-typing the URL each time. User specifically wanted this as an explicit on/off toggle, framed in their own words as a "description" switch: off = card opens the doc directly (the "Writing"-style pattern), on = card opens its own description page (the "Marketing"-style pattern). **Backfill preserved every existing project's actual current behavior exactly, not a single uniform default:** `showDescriptionPage` defaults `true` in the schema (matches "normal" behavior for any *new* project going forward), but a one-off script then set it `false` specifically for the 22 projects that already had a non-null `externalUrl` at migration time (confirmed via `updateMany({where: {externalUrl: {not: null}}, ...})`, run only after discovering — the same `migrate dev` doesn't reliably run `prisma generate` gotcha hit before, in this same session, with `moveSibling`/`moveProjectSibling` — that `npx prisma generate` had to be run explicitly before the backfill script could even see the new field). Card-routing logic in `category/[slug]/page.tsx` changed from `project.externalUrl || fallback` to `const opensDirectly = !project.showDescriptionPage && Boolean(project.externalUrl)` — deliberately still falls back to the description page if `showDescriptionPage` is false but `externalUrl` happens to be empty (nothing to open directly), rather than producing a dead/empty link. New checkbox lives inside the External URL card in `ProjectForm.tsx` (grouped with the URL/upload fields it actually controls, not a separate top-level "Featured"-style toggle), with explicit helper copy that flipping it doesn't touch the uploaded document itself. Verified live end-to-end via Playwright against the real running app: confirmed the checkbox defaults unchecked for a backfilled direct-open project (MoneyHub); confirmed clicking its card with the toggle off opens the in-app PDF modal (`role="dialog"`) and stays on the category page; toggled it on and confirmed clicking the *same* card now genuinely navigates to `/project/[id]` instead; toggled back off and confirmed the doc-modal behavior returns — proving the switch is fully reversible without re-touching the document — then restored MoneyHub to its original (unchecked) state to match the backfill.
- **Real gap in the `showDescriptionPage` feature above, caught by the user within minutes of using it (a genuine test project with an uploaded resume PDF, `showDescriptionPage: true`, and no `links[]` entries): the uploaded document became completely unreachable from the live site.** Root cause: `project/[id]/page.tsx` had only ever rendered `project.links[]` in its "Links" section — before this session's `showDescriptionPage` field existed, `externalUrl` never needed its own link on the detail page, because setting it *always* meant the card bypassed the detail page entirely (there was no code path where a project had both an attached `externalUrl` doc and a reachable `/project/[id]` page at the same time). Once the two were decoupled, that combination became possible for the first time and immediately orphaned the file — nothing on the whole site linked to it. Fixed by rendering a "View document" pill in the same Links section whenever `showDescriptionPage && externalUrl`, reusing the exact same `DocumentLink` component/styling already used for `links[]` entries (so it still opens a PDF in the in-app modal, exactly like every other document link on the site) — the "Links" section itself now also renders when this is the *only* thing to show (previously gated purely on `links.length > 0`). Verified live: the exact project the user was looking at (`cmt2uj9np0000fkf8jzx106yf`) now shows the "Links" section with a working "View document" pill that opens the attached resume PDF in the modal, confirmed by clicking it for real via Playwright rather than just checking the HTML contains the URL string (which wouldn't prove anything, since `DocumentLink` renders a JS-driven `<button>` for PDFs, not a plain `<a href>`).
- **Real bug: saving a project after uploading 6 gallery images threw `PrismaClientKnownRequestError` — "Transaction API error: Unable to start a transaction in the given time" — on `updateProject`'s `prisma.$transaction([...])` in `src/lib/projects.ts`.** Confirmed via the installed Prisma client's own `.d.ts` (not guessed) that this specific error text is Prisma's message for exceeding `maxWait` — the timeout for *acquiring a connection to start* a transaction, distinct from `timeout` (how long the transaction body itself may run) — and that Prisma's defaults are `maxWait: 2000`/`timeout: 5000` (both in ms). Root cause: **Neon's free-tier compute auto-suspends after a few minutes of DB inactivity, and waking it back up on the next query can take longer than that 2-second default** — `updateProjectAction` uploads cover/gallery/doc files to Blob (no DB activity at all during that phase) *before* calling `updateProject`, so a slow multi-file upload (6 real images) is exactly the kind of gap that gives Neon room to suspend right before the write needs to start. Ruled out the two more obvious suspects first rather than assuming: checked for leftover Node processes from this session's many `taskkill /F` dev-server restarts (only one process was actually running — not a local pileup) and queried `pg_stat_activity` directly on the dev branch (only 3 connections total, all recent — not pool exhaustion either) before landing on the cold-start explanation, which the error text itself (a *start*-transaction timeout, not an execution timeout) pointed to directly. **Fix:** set `transactionOptions: { maxWait: 10000, timeout: 20000 }` on the `PrismaClient` constructor in `src/lib/prisma.ts` — a client-level default that applies to every `$transaction` call in the app, not just this one, since `moveSibling`/`moveProjectSibling`/`deleteCategory` all use `$transaction` too and are equally exposed to the same cold-start window. Couldn't force-reproduce the literal cold-start in verification (Neon's compute was already warm from active testing), but confirmed the fix doesn't regress normal operation — resubmitted the exact failing scenario (6 real gallery images through the real running form) end-to-end via Playwright, all 6 persisted correctly, ~3s total. **Lesson: an error message naming a specific phase ("start a transaction" vs. the transaction itself) is worth reading literally before assuming a generic timeout bump — it pointed straight at `maxWait`, a completely different Prisma setting than the more commonly-tuned `timeout`.**
- **Replaced the "Existing gallery images" removal list (a plain checkbox + raw Blob URL per row) with a real thumbnail picker, per direct user ask — they'd just been staring at a wall of unreadable `blob.vercel-storage.com/...` links with no way to tell which photo was which.** New `src/components/admin/GalleryImagePicker.tsx` (`'use client'`, same minimal-scoped-client-component pattern as `CategoryTreeSelect`): a responsive grid of real `<img>` thumbnails, click-to-select (red border + "Remove" overlay badge on selected ones), a live "N of M selected for removal" count, and Select all/Clear bulk buttons. Still drives the exact same `<input type="checkbox" name="removeGallery" value={url}>` the server action already reads (now controlled via React state instead of native, but same name/value/semantics) — this was a pure presentation upgrade, zero server-side changes needed. **Confirmation-before-destructive-submit, implemented without lifting state into ProjectForm or making the whole form a client component:** on mount, a `useEffect` finds the nearest `<form>` via `ref.current.closest('form')` and attaches a *native* `submit` event listener (bypassing React's synthetic event system, since the submit button lives outside this component's own JSX and ProjectForm itself stays a server component) that calls `window.confirm(...)` whenever anything is selected, and `preventDefault()`s the submission if the user cancels — otherwise the plain Server Action submit proceeds exactly as before. Verified live end-to-end via Playwright against a real project with real uploaded images: thumbnails render correctly (no more raw URL text), individual selection and the Select all/Clear buttons all update the live count correctly, canceling the confirm() dialog genuinely blocks the submission (stays on the edit page), accepting it submits and the selected images are actually gone afterward — confirmed by literally counting the remaining thumbnails, not just trusting a redirect happened. (One test-script false alarm along the way: the script assumed the test project had exactly 6 images, based on an earlier verification pass in this same session — it actually had 7, since the user had uploaded one more of their own real test photo in between; not a bug, just a stale assumption in the throwaway test script, resolved by checking the DB directly rather than guessing.)

---

## Draft history

- **README.md**
  1. Initial scaffold: tech stack, content model (flat categories), public pages, admin module, decisions-locked section, open questions.
  2. Added multi-level category nesting (`parentId`, tree semantics, folder-with-files-and-subfolders rule).
  3. Locked in Tiptap, Resend, email-based password reset; added "Decisions Locked In" section.
  4. Added `gallery[]` and `videoUrl` project fields, external video hosting note, and the "Seed content plan" section mapping real reviewed files to categories/projects.
  5. Added Resume as an admin-uploadable PDF + "Download Resume" link (About Me / Home), after deciding not to build a structured Experience data model.
- **todo.md** — created: 12 stages (0–11) from planning docs through seed content and deploy prep, each with a Build checklist and a Test Gate checklist. Stage 1's blockers (Postgres/Resend/Blob provisioning) resolved and checked off; Stage 1 itself built and its test gate passed (dev/build/lint all green).
- **memory.md** — created: this file.
- **Stage 1 scaffolding**: `create-next-app` (Next.js 16, TypeScript, App Router, Turbopack, Tailwind, ESLint) bootstrapped into a temp folder and merged into repo root (our README.md/todo.md/memory.md preserved, the tool's own generated README.md discarded); added Prettier; `package.json` renamed to `shruti-shahu-portfolio`; `.env` (real secrets, gitignored) and `.env.example` (stub) created.
- **Stage 2 database**: Prisma 7.9.1 + `@prisma/adapter-pg` installed; `prisma/schema.prisma` written with all 6 models; migration `20260804184045_init` applied to the real Neon DB; `src/lib/prisma.ts` client singleton; Vitest installed and `src/lib/prisma.test.ts` written (6 tests, all passing against the live DB). See Technical learnings above for the Prisma 7 architecture details this uncovered.
- **First push to GitHub** (`AwwwRyan/ShrutiShahuPortfolio`, public repo): `.gitignore` extended to exclude the two raw work-sample folders, the CV PDF, the root duplicate `Video Script bank .xlsx`, and `.claude/` (local tooling config) — none of these are meant for the public repo, confirmed by Shruti. Also fixed `.env*` unintentionally swallowing `.env.example` (added `!.env.example` negation) so the env-var documentation actually gets committed. Push initially failed with a 403 — the machine's active `gh`/git credential was a different GitHub account (`AryanSrivastavaNM`) than the repo owner (`AwwwRyan`); resolved by running `gh auth switch --user AwwwRyan`. Verified post-push via `gh api repos/.../contents` that the exclusions actually took.
- **Stage 3 auth**: `bcryptjs` + `next-auth@beta` (5.0.0-beta.32) installed; `src/lib/password.ts` (+3 unit tests); `prisma/seed.ts` seeds `AdminUser` from `ADMIN_EMAIL`/`ADMIN_PASSWORD`, wired into `prisma.config.ts`'s new `migrations.seed` field, run via `npx prisma db seed`; `src/auth.ts` (Credentials provider), `src/app/api/auth/[...nextauth]/route.ts`, `src/app/admin/login/page.tsx` (server-action form), `src/app/admin/page.tsx` (placeholder dashboard), `src/proxy.ts` (not `middleware.ts` — see Technical learnings). Full login/logout/protected-route flow verified via curl against the live dev server.
- **Stage 4 password reset**: `resend` SDK installed; `src/lib/passwordReset.ts` (token create/validate, +4 unit tests) and `src/lib/email.ts` (Resend wrapper); `src/app/admin/forgot-password/page.tsx` and `src/app/admin/reset-password/page.tsx`; `src/proxy.ts` updated to allow both new routes unauthenticated; "Forgot password?" link + post-reset success message added to the login page. Full flow verified against the real dev server and real Neon DB (token creation, reset, old-password-rejected, new-password-accepted), then the original password was restored via re-seeding. Surfaced a real limitation, not a bug: Resend's free tier can't actually deliver to Shruti's email yet (see Technical learnings) — this is an open decision for Shruti, not something silently patched over.
- **Stage 5 category admin**: `src/lib/slugify.ts` + `src/lib/categories.ts` (tree read, create/rename/move/reorder/delete with cascade-vs-move-contents modes, 3 layers of circular-reference guarding, +9 unit tests in `categories.test.ts`); `src/app/admin/categories/page.tsx` (recursive tree UI, all plain-form server actions) and `src/app/admin/categories/[id]/delete/page.tsx` (confirmation flow); link added from the admin dashboard. Verified live: built and confirmed a real 3-level tree via HTTP, checked both delete-confirmation branches, executed a real cascade delete, cleaned up all test data afterward. Caught and fixed a static-rendering bug (see Technical learnings) before it could ship.
- **Stage 6 project admin**: `@vercel/blob`, `@tiptap/react` + `@tiptap/starter-kit` + `@tiptap/pm` installed; `src/lib/blob.ts` (upload helper), `src/lib/projects.ts` (full CRUD + reorder + featured toggle, +8 unit tests), `src/lib/projectFormData.ts` (shared FormData parsing for create/edit), `src/components/RichTextEditor.tsx` (Tiptap client component) and `src/components/ProjectForm.tsx` (shared create/edit form); `src/app/admin/projects/new`, `.../[id]/edit`, `.../[id]/delete`; project lists + reorder/feature controls wired into `/admin/categories`; `CategoryTreeNode` extended with per-category project summaries. Verified live end-to-end with **real image uploads to the real Vercel Blob store** (not mocked) — cover image, 2-image gallery, videoUrl, 2 links, tags, client, and featured all round-tripped correctly through 7 real test projects, then reorder and un-feature were exercised and verified before cascade-deleting all test data. Surfaced and deliberately deferred one gap (orphaned Blob images on delete) and hit a genuinely strange curl/shell quoting bug along the way — both in Technical learnings.
- **Stage 7 public pages**: `src/lib/categories.ts` extended with `getCategoryBreadcrumbs`, `listTopLevelCategories`, `getCategoryBySlug` (+5 unit tests); new `src/lib/siteContent.ts` (+2 tests) and `src/lib/youtube.ts` (watch/shorts/youtu.be → embed URL, +6 tests); `getProject` extended to include `category`. Built `src/app/page.tsx` (Home), `src/app/category/[slug]/page.tsx`, `src/app/project/[id]/page.tsx`, `src/app/contact/page.tsx` — the site's first real public-facing pages. Verified live end-to-end: seeded a genuine 3-level category tree + a fully-loaded project (real Blob images, YouTube embed, 2 links) directly via lib calls (not through the admin HTML forms — see Technical learnings on why), confirmed every field renders correctly including the exact breadcrumb trail at the deepest level, then cleaned up all test data and confirmed the Home page correctly falls back to its "coming soon" state. Caught two issues before shipping: two more static-rendering misses (same Stage 5 class of bug) and 3 lint errors from plain `<a>` tags that should have been `next/link`'s `<Link>`.
- **Stage 8 contact form**: `src/lib/contact.ts` (validation, +5 unit tests) and `sendContactMessage` added to `src/lib/email.ts` (with HTML-escaping and explicit Resend `error` checking — see Technical learnings); `/contact` extended with the actual form + Server Action (validation-error/send-failure/success states, each with a distinct message). Verified live: invalid email correctly rejected server-side via a real bypassing `curl` POST; a fully valid submission correctly surfaced a real error rather than a false "sent" confirmation, traced via server logs to the same known Resend recipient restriction from Stage 4; separately proved the Resend integration itself is fully correct by sending a one-off email to the account's own allowed address (real success, message id returned). No new code bugs — the only remaining blocker is the same Resend domain-verification decision already deferred at Stage 4, and the form will start working the moment that's resolved.
- **Stage 9 About Me/resume/contact-info admin**: `src/lib/blob.ts`'s `uploadImage` renamed to `uploadFile` (genuinely generic, now used for the resume PDF too); `src/lib/siteContent.ts` extended with `updateSiteContent` (+`SOCIAL_LINK_SLOTS = 6`, same fixed-slots pattern as project links, +2 more unit tests); `siteContent.test.ts` rewritten with proper snapshot/restore (see Technical learnings). New `/admin/site-content` page: one form for About Me (Tiptap), profile photo, resume PDF, contact email, and social links — folding in the README-specified "edit contact info" capability that `todo.md`'s Stage 9 checklist had omitted (see Technical learnings). Home page now also renders the profile photo. Verified live end-to-end with real uploads: About Me + photo appeared on Home immediately; resume uploaded and served at the correct Blob URL from the public Download Resume link; replaced the resume with a second PDF and confirmed the link updated (single-field, can't duplicate) while the untouched profile photo correctly stayed put, proving the per-field "keep existing if nothing new was uploaded" merge logic; contact email + a LinkedIn social link round-tripped to the public Contact page. All test data cleaned up afterward.
- **Stage 10 real content seeding** — this is the one stage whose output is meant to stay permanently, not get cleaned up. Wrote a one-off `tmp_seed_stage10.ts` (deleted after running) that uploaded every real source file to Blob directly via `put()` + `Buffer` (see Technical learnings), built the real 10-category tree, created all 26 real projects, and set the real `SiteContent` (About Me from the CV, `contactEmail: sshahu834@gmail.com` — note this is Shruti's **public/professional** email from the CV, deliberately different from `shruti2004shahu@gmail.com` which is only the private admin-login credential — and the LinkedIn social link). Final tree:
  - **Writing** (0 direct) → History (3: Wild West long-read, Romanovs, Guangxi sinkhole archaeology) · Culture (4: Tradwife trend piece, "Nobody Has a Personal Style Anymore," entertainment "bad guys" listicle, Tarek El Moussa bio) · Science & Psychology (5: psychology/therapy collection, AI & future of work, hybrid work, office energy trackers, digital money habits) · Academic (2: postcolonial Butalia analysis, course writing sample)
  - **Editing** (3, flat): Institute for Sexual Research before/after sample, MoneyHub EMI Calculator annotated draft, BizOps skills guide
  - **Marketing** (0 direct) → Copywriting (2: Investimate pitch deck, 18MB Creative Portfolio) · Video/UGC Scripts (6: all real YouTube-embedded videos from the confirmed URL table above)
  - **Digital Journalism** (1, flat): Digital News Roundup, 9-image gallery (cover + all 8 source photos)
  Verified exhaustively live: every category/subcategory renders with correct breadcrumbs, every project's assets (doc links, video embed, gallery images, the 18MB PDF) resolve with real 200s, admin-side project counts per category cross-checked against the seed numbers (exact match, sums to 26), and a full `build`+`lint`+`test` regression passed clean against the now-permanently-populated database. See the new Decision #23 for the category-assignment judgment calls this required.
